# Proactive Monitor 脳統合設計書

**バージョン:** v1.0.0
**作成日:** 2026-01-29
**作成者:** Claude Code（経営参謀・SE・PM）
**ステータス:** 設計中

---

## 1. 背景と問題

### 1.1 CLAUDE.md 脳の7つの鉄則（再掲）

| # | 鉄則 | 説明 |
|---|------|------|
| 1 | **全入力は脳を通る** | 例外なし。バイパス禁止。 |
| 2 | **脳は全記憶にアクセス** | 会話履歴、人物情報、ナレッジ、タスク、ユーザーの好み |
| 3 | **脳が判断、機能は実行** | 機能に判断ロジックを持たせない |
| 4 | **新機能追加しても脳構造は変えない** | 機能カタログへの追加のみで対応 |
| 5 | **確認が必要かは脳が決める** | 曖昧な場合は脳が確認を判断 |
| 6 | **状態管理は脳が統一管理** | 各機能が独自状態を持たない |
| 7 | **速度より正確性を優先** | 遅くても正確な応答を返す |

### 1.2 現状のProactive Monitorの問題

```
現状（鉄則違反）:
┌─────────────────────────────────────────────────┐
│ ProactiveMonitor                                │
│                                                 │
│  トリガー検知 → メッセージ判断 → メッセージ生成 → 送信  │
│                 ↑              ↑                │
│                 │              │                │
│              【違反】       【違反】             │
│            鉄則3違反       鉄則3違反             │
│         （機能が判断）   （機能が生成）          │
└─────────────────────────────────────────────────┘
```

**違反している鉄則:**

| 鉄則 | 違反内容 |
|------|---------|
| 3 | ProactiveMonitor自身が「どのメッセージを送るか」を判断 |
| 3 | ProactiveMonitor自身がメッセージを生成（テンプレート選択） |
| 2 | 脳が持つ記憶（過去の会話、ユーザーの好み）を参照していない |
| 5 | 送るべきかどうかの最終判断を脳が行っていない |

---

## 2. あるべき姿

### 2.1 アーキテクチャ（脳統合後）

```
あるべき姿（鉄則準拠）:
┌─────────────────┐      ┌─────────────────────────────────┐
│ ProactiveMonitor│      │           SoulkunBrain           │
│                 │      │                                  │
│  トリガー検知 ──┼─────→│ コンテキスト取得（記憶参照）     │
│                 │      │        ↓                        │
│                 │      │ 状況理解（なぜ声をかけるべきか）│
│                 │      │        ↓                        │
│                 │      │ 判断（送るべきか？何を送るか？）│
│                 │      │        ↓                        │
│  送信実行 ←────┼──────│ メッセージ生成（自然な言葉で）  │
│                 │      │                                  │
└─────────────────┘      └─────────────────────────────────┘
     【機能】                       【脳】
   （実行のみ）              （記憶・理解・判断・生成）
```

### 2.2 責務分離

| コンポーネント | 責務 | やること | やらないこと |
|---------------|------|---------|-------------|
| **ProactiveMonitor** | トリガー検知 + 送信実行 | 条件チェック、クールダウン、ChatWork API呼び出し | メッセージ内容の判断、メッセージ生成 |
| **SoulkunBrain** | 判断 + 生成 | 記憶参照、状況理解、送信判断、メッセージ生成 | トリガー条件チェック、API呼び出し |

---

## 3. 詳細設計

### 3.1 新規メソッド: SoulkunBrain.generate_proactive_message()

```python
async def generate_proactive_message(
    self,
    trigger_type: str,
    trigger_details: Dict[str, Any],
    user_id: str,
    organization_id: str,
    room_id: str,
    account_id: str,
) -> ProactiveMessageResult:
    """
    能動的メッセージを生成する（脳経由）

    【処理フロー】
    1. 記憶層: ユーザーのコンテキスト取得
       - 過去の会話履歴
       - ユーザーの好み・性格
       - 最近の感情傾向
       - 直近のやり取り

    2. 理解層: トリガー状況の理解
       - なぜこのトリガーが発火したか
       - ユーザーにとってどういう状況か
       - どのようなアプローチが適切か

    3. 判断層: 送信判断
       - 今このタイミングで送るべきか
       - どのようなトーンで送るべきか
       - 何を伝えるべきか

    4. 生成層: メッセージ生成
       - ユーザーの好みに合わせた言葉遣い
       - 状況に応じた内容
       - ソウルくんらしい表現

    Args:
        trigger_type: トリガータイプ（goal_abandoned, task_overload等）
        trigger_details: トリガーの詳細情報
        user_id: ユーザーID
        organization_id: 組織ID
        room_id: ChatWorkルームID
        account_id: ChatWorkアカウントID

    Returns:
        ProactiveMessageResult:
            - should_send: 送るべきか（脳の判断）
            - message: 生成されたメッセージ（送る場合）
            - reason: 判断理由
            - debug_info: デバッグ情報
    """
```

### 3.2 ProactiveMessageResult データクラス

```python
@dataclass
class ProactiveMessageResult:
    """脳による能動的メッセージ生成結果"""

    should_send: bool
    """送信すべきか（脳の判断）"""

    message: Optional[str]
    """生成されたメッセージ（should_send=Trueの場合）"""

    reason: str
    """判断理由（ログ・デバッグ用）"""

    confidence: float
    """判断の確信度（0.0-1.0）"""

    tone: str
    """選択されたトーン（friendly, encouraging, concerned等）"""

    debug_info: Dict[str, Any]
    """デバッグ情報"""
```

### 3.3 ProactiveMonitor._take_action() の修正

```python
async def _take_action(
    self,
    trigger: Trigger,
    user_ctx: UserContext,
) -> ProactiveAction:
    """アクションを実行（脳経由）"""

    # 脳にメッセージ生成を依頼
    brain_result = await self._brain.generate_proactive_message(
        trigger_type=trigger.trigger_type.value,
        trigger_details=trigger.details,
        user_id=user_ctx.user_id,
        organization_id=user_ctx.organization_id,
        room_id=user_ctx.dm_room_id,
        account_id=user_ctx.chatwork_account_id,
    )

    # 脳が「送らない」と判断した場合
    if not brain_result.should_send:
        logger.info(f"[Proactive] Brain decided not to send: {brain_result.reason}")
        return ProactiveAction(
            message=ProactiveMessage(
                trigger=trigger,
                message_type=self._get_message_type(trigger.trigger_type),
                message="",
                room_id=user_ctx.dm_room_id,
                account_id=user_ctx.chatwork_account_id,
            ),
            success=True,
            skipped=True,
            skip_reason=brain_result.reason,
        )

    # 脳が生成したメッセージを送信
    message = brain_result.message

    # dry_runモードの場合はログのみ
    if self._dry_run:
        logger.info(f"[Proactive][DRY_RUN] Would send (brain-generated): {message}")
        return ProactiveAction(message=proactive_msg, success=True)

    # 実際に送信
    # ... (送信ロジック)
```

### 3.4 脳が参照する記憶

| 記憶タイプ | 取得元 | 活用方法 |
|-----------|--------|---------|
| 過去の会話履歴 | BrainMemoryAccess.get_conversation_history() | 最近の話題、文脈 |
| ユーザーの好み | BrainMemoryAccess.get_user_preferences() | 好みの言葉遣い、トーン |
| 人物情報 | BrainMemoryAccess.get_person_info() | 名前、役職、部署 |
| 感情傾向 | emotion_scores テーブル | 最近の感情状態 |
| 過去の能動的メッセージ | brain_proactive_actions テーブル | 前回何を送ったか |

---

## 4. 実装計画

### 4.1 ステップ

| # | ステップ | 内容 | 影響範囲 |
|---|---------|------|---------|
| 1 | モデル追加 | ProactiveMessageResultをlib/brain/models.pyに追加 | models.py |
| 2 | 脳メソッド追加 | generate_proactive_message()をcore.pyに追加 | core.py |
| 3 | ProactiveMonitor修正 | _take_action()を脳経由に変更 | proactive.py |
| 4 | テスト作成 | 脳統合のテスト | tests/ |
| 5 | chatwork-webhook同期 | 変更を同期 | chatwork-webhook/ |
| 6 | proactive-monitor同期 | 変更を同期 | proactive-monitor/ |
| 7 | 動作確認 | ドライランで確認 | - |
| 8 | 本番有効化 | PROACTIVE_DRY_RUN=false | Cloud Function |

### 4.2 依存関係

```
Step 1 → Step 2 → Step 3 → Step 4 → Step 5 → Step 6 → Step 7 → Step 8
  │         │         │         │
  └─────────┴─────────┴─────────┴─ 順次実行（依存あり）
```

---

## 5. メッセージ生成のAIプロンプト設計

### 5.1 プロンプト構造

```
【状況】
- ユーザー: {user_name}さん（{department}、{position}）
- トリガー: {trigger_type}
- 詳細: {trigger_details}

【記憶からの情報】
- 最近の会話: {recent_conversations}
- ユーザーの好み: {preferences}
- 最近の感情傾向: {emotion_trend}
- 前回の能動的メッセージ: {last_proactive_message}

【判断基準】
1. 今このタイミングで声をかけるべきか？
2. どのようなトーンが適切か？（励まし/心配/お祝い/リマインド）
3. 具体的に何を伝えるべきか？

【制約】
- ソウルくんのキャラクター（語尾は「ウル」、絵文字は🐺）
- 押し付けがましくない
- 相手の状況を尊重する
- 簡潔に（3文以内）

【出力形式】
{
  "should_send": true/false,
  "reason": "判断理由",
  "message": "生成されたメッセージ（should_send=trueの場合）",
  "tone": "選択したトーン"
}
```

---

## 6. テスト計画

### 6.1 テストケース

| # | テストケース | 期待結果 |
|---|-------------|---------|
| 1 | 目標放置（7日） + ポジティブな会話履歴 | 励まし調のメッセージ |
| 2 | 目標放置（7日） + ネガティブな感情傾向 | 心配・共感調のメッセージ |
| 3 | タスク山積み + 忙しそうな会話履歴 | 簡潔なリマインド |
| 4 | 目標達成 | お祝いメッセージ |
| 5 | 前回同じトリガーで送信済み（クールダウン中） | should_send=false |
| 6 | ユーザーが「声かけ不要」と設定 | should_send=false |

---

## 7. リスクと対策

| リスク | 影響 | 対策 |
|--------|------|------|
| AI生成メッセージの品質 | 不適切な表現 | プロンプトで制約、テンプレートのフォールバック |
| レスポンス時間増加 | 送信遅延 | タイムアウト設定、非同期処理 |
| APIコスト増加 | 運用コスト | Economy tierの使用、キャッシュ |

---

## 8. 効果

### 8.1 Before/After

| 項目 | Before | After |
|------|--------|-------|
| メッセージ内容 | テンプレート固定 | 状況に応じた自然な言葉 |
| 送信判断 | クールダウンのみ | 脳が文脈を考慮して判断 |
| ユーザー体験 | 機械的 | 人間の秘書のような声かけ |
| CLAUDE.md準拠 | ❌ 違反 | ✅ 準拠 |

### 8.2 CLAUDE.md鉄則チェック

| 鉄則 | Before | After |
|------|--------|-------|
| 3. 脳が判断、機能は実行 | ❌ | ✅ |
| 2. 脳は全記憶にアクセス | ❌ | ✅ |
| 5. 確認が必要かは脳が決める | ❌ | ✅ |
