name: Quality Checks

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main]

jobs:
  # =====================================================
  # 1. lib/コピーの同期チェック（v10.53.0拡張）
  # =====================================================
  #
  # チェック対象:
  #   - lib/text_utils.py
  #   - lib/goal_setting.py
  #   - lib/mvv_context.py
  #   - lib/report_generator.py
  #   - lib/audit.py
  #   - lib/memory/* (ディレクトリ)
  #   - lib/detection/* (ディレクトリ)
  #   - lib/business_day.py
  #   - lib/brain/* (ディレクトリ) ← v10.53.0追加【重要】
  #   - lib/feature_flags.py ← v10.53.0追加
  #
  # =====================================================
  lib-sync-check:
    name: Check lib/ copies are in sync
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Check all lib/ copies are in sync
        run: |
          echo "🔍 Checking all lib/ copies are in sync..."
          echo ""
          # sync_lib.sh --check uses rsync mirror for chatwork-webhook and proactive-monitor
          ./scripts/sync_lib.sh --check

  # =====================================================
  # SQL Column Validation (B-5: --no-verify bypass protection)
  # =====================================================
  sql-column-check:
    name: Validate SQL column references
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Validate SQL columns against schema
        run: |
          echo "🔍 Validating SQL column references against db_schema.json..."
          echo ""
          ./scripts/validate_sql_columns.sh --all

  # =====================================================
  # 2. デプロイ設定の整合性チェック（v10.53.1追加）
  # =====================================================
  #
  # 背景:
  #   環境変数名の不一致（ENABLE_LLM_BRAIN vs USE_BRAIN_ARCHITECTURE）で
  #   LLM Brainが無効化される問題が発生した。
  #   この検証で再発を防止する。
  #
  deploy-config-check:
    name: Check deploy configuration consistency
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Validate deploy configuration
        run: |
          echo "🔍 Checking deploy configuration consistency..."
          echo ""

          # env_config.py から正しい環境変数名を取得
          ENV_CONFIG_FILE="lib/brain/env_config.py"

          if [ ! -f "$ENV_CONFIG_FILE" ]; then
            echo "❌ $ENV_CONFIG_FILE not found"
            exit 1
          fi

          # ENV_BRAIN_ENABLED の値を抽出
          EXPECTED_VAR=$(grep 'ENV_BRAIN_ENABLED = ' "$ENV_CONFIG_FILE" | sed 's/.*= "\(.*\)"/\1/')
          echo "Expected environment variable: $EXPECTED_VAR"
          echo ""

          ERRORS_FOUND=0

          # 各デプロイファイルをチェック
          DEPLOY_FILES=(
            "cloudbuild.yaml"
            "cloudbuild-proactive-monitor.yaml"
            "chatwork-webhook/deploy.sh"
            "proactive-monitor/deploy.sh"
          )

          for file in "${DEPLOY_FILES[@]}"; do
            if [ -f "$file" ]; then
              echo "Checking $file..."

              # 正しい変数名が含まれているか
              if grep -q "$EXPECTED_VAR=true" "$file"; then
                echo "  ✅ $EXPECTED_VAR=true found"
              else
                echo "  ❌ $EXPECTED_VAR=true NOT found"
                ERRORS_FOUND=1
              fi

              # 間違った変数名が含まれていないか
              for wrong_var in "ENABLE_LLM_BRAIN" "LLM_BRAIN_ENABLED" "BRAIN_ENABLED"; do
                if grep -q "$wrong_var" "$file"; then
                  echo "  ❌ Wrong variable name '$wrong_var' found"
                  ERRORS_FOUND=1
                fi
              done

              # 認証設定: chatwork-webhookは公開（HMAC検証済み）、それ以外は認証必須
              case "$file" in
                cloudbuild.yaml|chatwork-webhook/*)
                  # Webhook受信サービスは--allow-unauthenticated必須
                  if grep -q "\-\-no-allow-unauthenticated" "$file"; then
                    echo "  ❌ Webhook service should use --allow-unauthenticated"
                    ERRORS_FOUND=1
                  fi
                  ;;
                *)
                  # 内部サービスは--no-allow-unauthenticated必須（Task 1-2: JWT認証統一）
                  if grep -q "\-\-allow-unauthenticated" "$file" && ! grep -q "\-\-no-allow-unauthenticated" "$file"; then
                    echo "  ❌ Internal service should use --no-allow-unauthenticated"
                    ERRORS_FOUND=1
                  fi
                  ;;
              esac
            fi
          done

          # feature_flags.py もチェック（間違った変数名を参照していないか）
          echo ""
          echo "Checking lib/feature_flags.py..."
          FF_FILE="lib/feature_flags.py"
          if [ -f "$FF_FILE" ]; then
            # enable_llm_brain の設定で USE_BRAIN_ARCHITECTURE を使用しているか
            # （コードが複数行にわたる場合も考慮）
            if grep -A1 "self.enable_llm_brain" "$FF_FILE" | grep -q "\"$EXPECTED_VAR\""; then
              echo "  ✅ enable_llm_brain uses $EXPECTED_VAR"
            else
              echo "  ❌ enable_llm_brain does not use $EXPECTED_VAR"
              ERRORS_FOUND=1
            fi
            # ENABLE_LLM_BRAIN が使われていないか（ドキュメント/コメント以外で）
            # 除外: コメント行（#で始まる）、docstring内、文字列リテラル内の説明
            if grep "ENABLE_LLM_BRAIN" "$FF_FILE" | grep -v "^#" | grep -v "# " | grep "_get_env_bool" | grep -q .; then
              echo "  ❌ Uses wrong variable name 'ENABLE_LLM_BRAIN' in _get_env_bool"
              ERRORS_FOUND=1
            fi
          fi

          echo ""
          if [ $ERRORS_FOUND -eq 1 ]; then
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "❌ Deploy configuration errors found!"
            echo ""
            echo "Fix:"
            echo "  1. Use '$EXPECTED_VAR' (not ENABLE_LLM_BRAIN)"
            echo "  2. Webhook services: --allow-unauthenticated / Internal services: --no-allow-unauthenticated"
            echo "  3. feature_flags.py must use '$EXPECTED_VAR' for enable_llm_brain"
            echo ""
            echo "Reference: lib/brain/env_config.py is the Single Source of Truth"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            exit 1
          fi

          echo "✅ All deploy configurations are consistent!"

  # =====================================================
  # 3. 禁止パターン検出（直接切り詰め防止）
  # =====================================================
  #
  # 検出対象: タスク表示用の直接切り詰め（[:30], [:40]形式）
  # 検出対象外:
  #   - ログ出力用（[:500]など大きな数字）
  #   - エラーメッセージ（str(e)[:50]）
  #   - summary_preview（ログ出力用）
  #   - フォールバック関数内の定義
  #
  # v10.53.1: 番号を2→3に変更（deploy-config-check追加のため）
  #
  forbidden-patterns-check:
    name: Check for forbidden patterns
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Check for direct truncation patterns
        run: |
          echo "🔍 Checking for forbidden truncation patterns..."
          echo "   (Checking for task display truncation, not log output)"

          # 検出対象ファイル
          TARGET_FILES=(
            "remind-tasks/main.py"
            "sync-chatwork-tasks/main.py"
          )

          # =====================================================
          # 禁止パターン（v10.17.1強化）
          # =====================================================
          # 検出対象:
          #   - body[:30], body[:40] + "..." 形式
          #   - task["body"][:30] + "..." 形式
          #   - task_body[:30] + "..." 形式
          #   - clean_task_name[:30] + "..." 形式
          #   - clean_body[:40] + "..." 形式
          #
          # 除外パターン:
          #   - summary[:30]（ログ出力用なのでOK）
          #   - summary_preview（ログ出力用なのでOK）
          #   - current_summary[:50]（ログ/監査用なのでOK）
          #   - text[:max_length]（フォールバック関数内なのでOK）
          # =====================================================

          ERRORS_FOUND=0

          for file in "${TARGET_FILES[@]}"; do
            if [ -f "$file" ]; then
              echo "Checking $file..."

              # パターン1: body[:30] + "..." 形式（変数名がbodyで始まる）
              # ただし、"summary" を含む行は除外
              if grep -n 'body\[:3[0-9]\].*\.\.\.' "$file" | grep -v "summary" > /tmp/matches.txt 2>/dev/null; then
                if [ -s /tmp/matches.txt ]; then
                  echo "❌ Found dangerous body truncation pattern in $file:"
                  cat /tmp/matches.txt
                  ERRORS_FOUND=1
                fi
              fi

              # パターン2: task["body"][:30] + "..." 形式
              if grep -n 'task\["body"\]\[:3[0-9]\].*\.\.\.' "$file" > /tmp/matches.txt 2>/dev/null; then
                if [ -s /tmp/matches.txt ]; then
                  echo "❌ Found dangerous task[\"body\"] truncation pattern in $file:"
                  cat /tmp/matches.txt
                  ERRORS_FOUND=1
                fi
              fi

              # パターン3: task_info["body"][:30] + "..." 形式
              if grep -n 'task_info\["body"\]\[:3[0-9]\].*\.\.\.' "$file" > /tmp/matches.txt 2>/dev/null; then
                if [ -s /tmp/matches.txt ]; then
                  echo "❌ Found dangerous task_info[\"body\"] truncation pattern in $file:"
                  cat /tmp/matches.txt
                  ERRORS_FOUND=1
                fi
              fi

              # パターン4: task_body[:30] パターン（f-string内）
              if grep -n 'task_body\[:3[0-9]\]' "$file" > /tmp/matches.txt 2>/dev/null; then
                if [ -s /tmp/matches.txt ]; then
                  echo "❌ Found dangerous task_body truncation pattern in $file:"
                  cat /tmp/matches.txt
                  ERRORS_FOUND=1
                fi
              fi

              # パターン5: clean_task_name[:30] + "..." 形式
              if grep -n 'clean_task_name\[:3[0-9]\].*\.\.\.' "$file" > /tmp/matches.txt 2>/dev/null; then
                if [ -s /tmp/matches.txt ]; then
                  echo "❌ Found dangerous clean_task_name truncation pattern in $file:"
                  cat /tmp/matches.txt
                  ERRORS_FOUND=1
                fi
              fi

              # パターン6: clean_body[:4x] + "..." 形式（元々のチェック）
              if grep -n 'clean_body\[:4[0-9]\].*\.\.\.' "$file" > /tmp/matches.txt 2>/dev/null; then
                if [ -s /tmp/matches.txt ]; then
                  echo "❌ Found dangerous clean_body truncation pattern in $file:"
                  cat /tmp/matches.txt
                  ERRORS_FOUND=1
                fi
              fi
            fi
          done

          if [ $ERRORS_FOUND -eq 1 ]; then
            echo ""
            echo "❌ Forbidden patterns found!"
            echo "   Use prepare_task_display_text() instead of direct truncation."
            exit 1
          fi

          echo "✅ No forbidden truncation patterns found!"

      - name: Check for debug print statements
        run: |
          echo "🔍 Checking for debug print statements..."
          echo "   (Debug prints should not be committed to production code)"
          echo ""

          # 検出対象ファイル（全main.pyファイル）
          TARGET_FILES=(
            "main.py"
            "chatwork-webhook/main.py"
            "sync-chatwork-tasks/main.py"
            "remind-tasks/main.py"
            "check-reply-messages/main.py"
            "cleanup-old-data/main.py"
            "pattern-detection/main.py"
          )

          ERRORS_FOUND=0

          for file in "${TARGET_FILES[@]}"; do
            if [ -f "$file" ]; then
              echo "Checking $file..."

              # パターン1: 🔍 DEBUG: パターン（明らかなデバッグ用）
              if grep -n '🔍 DEBUG:' "$file" > /tmp/debug_matches.txt 2>/dev/null; then
                if [ -s /tmp/debug_matches.txt ]; then
                  echo "❌ Found debug print in $file:"
                  cat /tmp/debug_matches.txt
                  ERRORS_FOUND=1
                fi
              fi

              # パターン2: 受信データ全体をログ出力（セキュリティリスク）
              if grep -n '受信データ全体' "$file" > /tmp/debug_matches.txt 2>/dev/null; then
                if [ -s /tmp/debug_matches.txt ]; then
                  echo "❌ Found full request data logging in $file:"
                  cat /tmp/debug_matches.txt
                  ERRORS_FOUND=1
                fi
              fi

              # パターン3: json.dumps(data) をそのままprint（セキュリティリスク）
              if grep -n 'print.*json\.dumps(data' "$file" > /tmp/debug_matches.txt 2>/dev/null; then
                if [ -s /tmp/debug_matches.txt ]; then
                  echo "❌ Found raw data dump in $file:"
                  cat /tmp/debug_matches.txt
                  ERRORS_FOUND=1
                fi
              fi
            fi
          done

          if [ $ERRORS_FOUND -eq 1 ]; then
            echo ""
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "❌ Debug print statements found!"
            echo ""
            echo "These patterns are forbidden in production code:"
            echo "  - print(f\"🔍 DEBUG: ...\") - Use logging instead"
            echo "  - print(f\"🔍 受信データ全体: ...\") - Security risk"
            echo "  - print(json.dumps(data)) - Security risk"
            echo ""
            echo "Remove these debug statements before merging."
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            exit 1
          fi

          echo "✅ No debug print statements found!"

  # =====================================================
  # 4. ユニットテスト実行
  # =====================================================
  unit-tests:
    name: Run unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest

      - name: Run text_utils tests
        run: |
          echo "🧪 Running lib/text_utils.py unit tests..."
          # テストをルートディレクトリから実行（lib/へのパスを解決）
          cd $GITHUB_WORKSPACE
          PYTHONPATH=$GITHUB_WORKSPACE python -m pytest tests/test_text_utils_lib.py -v --tb=short --ignore-glob='**/embedding.py'
          echo "✅ All tests passed!"

  # =====================================================
  # 5. 型チェック（mypy）
  # =====================================================
  #
  # 対象: lib/brain/ の主要ファイル
  # 目的: 型の不整合による本番障害を防止
  #
  # =====================================================
  type-check:
    name: Type Check (mypy)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mypy types-requests

      - name: Run mypy on lib/brain/
        run: |
          echo "🔍 Running mypy type check on lib/brain/..."
          echo ""

          # Phase 1: models.pyとtype_safety.pyのみを厳格チェック
          # --follow-imports=skip で依存ファイルのエラーを無視
          mypy lib/brain/models.py lib/brain/type_safety.py \
            --ignore-missing-imports \
            --follow-imports=skip \
            --no-error-summary \
            --show-error-codes \
            || {
              echo ""
              echo "❌ Type errors found in lib/brain/models.py or type_safety.py"
              echo "Please fix type annotations before merging."
              exit 1
            }

          echo ""
          echo "✅ Type check passed (models.py, type_safety.py)"
          echo ""
          echo "📋 Note: Other files in lib/brain/ will be checked in future phases."

  # =====================================================
  # 6. 非同期パターン検証（v11.2.0追加: P11修正）
  # =====================================================
  #
  # CLAUDE.md §3-2 セクションF (#19-22) に対応:
  #   AP-01: asyncio.create_task が _fire_and_forget 外で使われていないか
  #   AP-02: wait_for(asyncio.to_thread) 禁止パターン
  #   AP-03: per-call httpx.AsyncClient() 禁止
  #   AP-04: str(e) PII漏洩チェック
  #   AP-05: shutdown(wait=False) 禁止
  #   AP-07: --set-env-vars 禁止
  #   AP-08: bare except:pass チェック
  #
  # =====================================================
  async-pattern-check:
    name: Validate async patterns (CLAUDE.md §3-2 F)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Run async pattern validator
        run: |
          echo "🔍 Validating async patterns and operational safety..."
          echo ""
          bash scripts/validate_async_patterns.sh

  # =====================================================
  # 最終ステータス
  # =====================================================
  quality-gate:
    name: Quality Gate
    runs-on: ubuntu-latest
    needs: [lib-sync-check, sql-column-check, deploy-config-check, forbidden-patterns-check, unit-tests, type-check, async-pattern-check]
    steps:
      - name: All checks passed
        run: |
          echo "✅ All quality checks passed!"
          echo ""
          echo "Checks completed:"
          echo "  - lib/ copies sync check (7 modules, 20+ targets)"
          echo "  - Deploy configuration consistency check"
          echo "  - Forbidden patterns check (truncation + debug prints)"
          echo "  - Unit tests"
          echo "  - Type check (mypy on lib/brain/)"
          echo "  - Async pattern validation (CLAUDE.md §3-2 F)"
