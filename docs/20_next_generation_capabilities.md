# 第20章: 次世代能力設計書

**バージョン:** v1.0.0
**作成日:** 2026-01-27
**最終更新:** 2026-01-27
**ステータス:** 設計確定

---

## 目次

1. [エグゼクティブサマリー](#1-エグゼクティブサマリー)
2. [ビジョンと目標](#2-ビジョンと目標)
3. [アーキテクチャ概要](#3-アーキテクチャ概要)
4. [Phase M: Multimodal（マルチモーダル）](#4-phase-m-multimodalマルチモーダル)
5. [Phase G: Generation（生成能力）](#5-phase-g-generation生成能力)
6. [Phase AA: Autonomous Agent（自律エージェント）](#6-phase-aa-autonomous-agent自律エージェント)
7. [Phase F: Feedback（CEOフィードバック）](#7-phase-f-feedbackceoフィードバック)
8. [スマートモデル管理システム](#8-スマートモデル管理システム)
9. [データベース設計](#9-データベース設計)
10. [実装ロードマップ](#10-実装ロードマップ)
11. [リスクと対策](#11-リスクと対策)
12. [統合アプリケーション](#12-統合アプリケーション)
13. [付録](#13-付録)

---

## 1. エグゼクティブサマリー

### 1.1 本設計書の目的

本設計書は、ソウルくんを「会社を一番理解し、デジタル上で何でもできるAI」に進化させるための次世代能力を定義する。

### 1.2 実現する価値

| 能力 | ビジネス価値 |
|------|-------------|
| **Multimodal** | 画像・PDF・URLを理解し、情報収集を自動化 |
| **Generation** | 資料・画像・動画を自動生成し、作業時間を削減 |
| **Autonomous Agent** | バックグラウンドで調査・分析を自律実行 |
| **Feedback** | 事実ベースでCEOの意思決定を支援 |
| **スマートモデル管理** | 常に最新AIを最適コストで利用 |

### 1.3 脳アーキテクチャとの関係

```
┌─────────────────────────────────────────────────────────────┐
│                    ソウルくんの脳（既存）                    │
│              意図理解 → 判断 → 実行 → 記憶                  │
└─────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              ↓               ↓               ↓
     ┌────────────┐   ┌────────────┐   ┌────────────┐
     │ 既存能力    │   │ 新能力      │   │ 脳の進化   │
     │ (タスク等)  │   │ (M/G/AA/F) │   │ (2F〜2O)  │
     └────────────┘   └────────────┘   └────────────┘
           ↑                ↑               ↑
        完了済み          本設計書         別軸で並行
```

**重要:** 新能力は脳の構造を変えずに追加できる。脳フェーズ（2F〜2O）とは完全に独立して開発可能。

### 1.4 工数サマリー

| Phase | 内容 | 工数 | 優先度 |
|-------|------|:----:|:------:|
| モデル管理 | スマートモデル管理システム | 3-4週間 | ★★★ |
| Phase M | Multimodal | 4-5週間 | ★★★ |
| Phase G | Generation | 5-6週間 | ★★☆ |
| Phase F | Feedback | 4-5週間 | ★★☆ |
| Phase AA | Autonomous Agent | 6-8週間 | ★☆☆ |
| **合計** | | **22-28週間** | |

---

## 2. ビジョンと目標

### 2.1 究極のソウルくん像

```
【入力】
  テキスト / 画像 / PDF / URL / Excel / 音声

【処理】
  理解 → 判断 → 実行 → 記憶 → 学習

【出力】
  テキスト / 資料作成 / 画像生成 / 動画生成 / 自律行動

【特性】
  ・会社のことを一番理解している
  ・全てのAI能力を統合（ChatGPT + Claude + Gemini + ...）
  ・新しいAIが出たら自動で取り込む
  ・事実に基づいてフィードバックをくれる
```

### 2.2 設計原則

| # | 原則 | 説明 |
|---|------|------|
| 1 | **統合 > 構築** | AIを自作せず、既存APIを賢く統合する |
| 2 | **会社文脈の付加** | 他のAIにない「会社の理解」を強みにする |
| 3 | **コスト意識** | 高品質と低コストを両立する仕組みを持つ |
| 4 | **段階的拡張** | 一度に全部作らず、価値の高いものから順に |
| 5 | **自己進化** | 新しいツールを自動で取り込める設計にする |

### 2.3 成功指標（KPI）

| 指標 | 現状 | 目標 | 測定方法 |
|------|------|------|---------|
| 対応可能な入力形式 | 1種類（テキスト） | 6種類 | 実装数 |
| 生成可能な出力形式 | 1種類（テキスト） | 5種類 | 実装数 |
| AIモデル利用数 | 2種類 | 10種類以上 | モデルレジストリ |
| CEOへのフィードバック | 0回/週 | 7回/週 | 配信ログ |
| 自律タスク実行 | 0件/週 | 10件/週 | 実行ログ |

---

## 3. アーキテクチャ概要

### 3.1 全体構成図

```
┌─────────────────────────────────────────────────────────────────────┐
│                         ソウルくんシステム                          │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                      入力層（Phase M）                        │ │
│  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐           │ │
│  │  │テキスト│ │画像 │ │PDF │ │URL │ │Excel│ │音声 │           │ │
│  │  └──┬──┘ └──┬──┘ └──┬──┘ └──┬──┘ └──┬──┘ └──┬──┘           │ │
│  │     └───────┴───────┴───────┴───────┴───────┘               │ │
│  │                           ↓                                   │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │              マルチモーダルプロセッサー                  │ │ │
│  │  │    画像解析 / PDF抽出 / Webスクレイピング / 音声認識    │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                │                                    │
│                                ↓                                    │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                    脳（SoulkunBrain）                         │ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐           │ │
│  │  │ 理解層  │→│ 判断層  │→│ 実行層  │→│ 記憶層  │           │ │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘           │ │
│  │                    ↓                                         │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │           モデルオーケストレーター                       │ │ │
│  │  │    タスク分析 → モデル選択 → コスト判定 → 実行          │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                │                                    │
│                                ↓                                    │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                     出力層（Phase G）                         │ │
│  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐                   │ │
│  │  │テキスト│ │資料 │ │画像 │ │動画 │ │音声 │                   │ │
│  │  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘                   │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                │                                    │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                 自律層（Phase AA）                            │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │   バックグラウンドタスク / ディープリサーチ / ツール連携  │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                │                                    │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                フィードバック層（Phase F）                    │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │  ファクト収集 → 分析 → フィードバック生成 → CEO配信     │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                    ツールレジストリ                           │ │
│  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐         │ │
│  │  │GPT-4o│ │Claude│ │Gemini│ │DALL-E│ │Runway│ │ ... │         │ │
│  │  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘         │ │
│  │                    ↑ 動的に追加可能                          │ │
│  └───────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 データフロー

```
【入力から出力まで】

1. ユーザー入力（ChatWork）
   │
   ├─ テキスト → そのまま脳へ
   ├─ 画像添付 → Phase M で解析 → テキスト化 → 脳へ
   ├─ ファイル添付 → Phase M で抽出 → テキスト化 → 脳へ
   └─ URL → Phase M でフェッチ → テキスト化 → 脳へ
   │
2. 脳で処理
   │
   ├─ 意図理解（何をしたいか）
   ├─ 記憶参照（関連する過去情報）
   ├─ 判断（どの能力を使うか）
   └─ モデル選択（どのAIを使うか）
   │
3. 実行
   │
   ├─ テキスト応答 → 直接返信
   ├─ 資料生成 → Phase G → Google Docs/PDF
   ├─ 画像生成 → Phase G → DALL-E → 画像ファイル
   ├─ リサーチ → Phase AA → バックグラウンド実行
   └─ フィードバック → Phase F → CEO向け配信
   │
4. 記憶・学習
   │
   └─ 全てのやり取りを記憶に保存
```

---

## 4. Phase M: Multimodal（マルチモーダル）

### 4.1 概要

ソウルくんが画像・PDF・URL・Excelなど、テキスト以外の入力を理解できるようにする。

### 4.2 対応フォーマット

| フォーマット | 処理方法 | 使用技術 | 優先度 |
|-------------|---------|---------|:------:|
| **画像** | Vision APIで解析 | GPT-4V / Gemini Vision | ★★★ |
| **PDF** | テキスト抽出 + Vision | PyPDF2 + GPT-4V | ★★★ |
| **URL** | Webスクレイピング | BeautifulSoup / Playwright | ★★★ |
| **Excel/CSV** | パース + 分析 | pandas + LLM | ★★☆ |
| **Word/PowerPoint** | テキスト抽出 | python-docx / python-pptx | ★★☆ |
| **音声** | 文字起こし | Whisper API | ★☆☆ |

### 4.3 画像理解（M1）

#### 4.3.1 ユースケース

```
【領収書の読み取り】
ユーザー: [画像添付] 「これ経費精算して」

ソウルくん:
「領収書を読み取ったウル！

📝 内容:
・店名: スターバックス 渋谷店
・日付: 2026/01/27
・金額: ¥1,280
・品目: コーヒー、サンドイッチ

経費精算システムに登録するウル？
勘定科目は「会議費」でいいウル？」
```

```
【図面・設計書の分析】
ユーザー: [画像添付] 「この設計、問題点ある？」

ソウルくん:
「設計図を分析したウル！

⚠️ 気になる点:
1. エラーハンドリングのフローが不足してるウル
2. DBアクセスが直列になってて遅くなりそう
3. 認証チェックがAPI入口にないウル

詳しく説明するウル？」
```

#### 4.3.2 技術仕様

```python
# lib/capabilities/multimodal/image_processor.py

class ImageProcessor:
    """画像処理プロセッサー"""

    SUPPORTED_FORMATS = ['jpg', 'jpeg', 'png', 'gif', 'webp']
    MAX_FILE_SIZE_MB = 20

    async def process(
        self,
        image_data: bytes,
        context: BrainContext,
        instruction: Optional[str] = None
    ) -> ImageAnalysisResult:
        """
        画像を解析してテキスト情報を抽出

        Args:
            image_data: 画像のバイナリデータ
            context: 脳のコンテキスト（会社情報等）
            instruction: 追加の指示（何を読み取るか）

        Returns:
            ImageAnalysisResult: 解析結果
        """
        # 1. 画像の前処理（リサイズ、フォーマット変換）
        processed_image = self._preprocess(image_data)

        # 2. モデル選択（コスト vs 精度）
        model = await self.model_orchestrator.select_vision_model(
            task_type="image_analysis",
            complexity=self._estimate_complexity(processed_image)
        )

        # 3. Vision APIで解析
        analysis = await self._call_vision_api(
            model=model,
            image=processed_image,
            instruction=instruction,
            company_context=context.company_knowledge
        )

        # 4. 結果を構造化
        return ImageAnalysisResult(
            description=analysis.description,
            extracted_text=analysis.ocr_text,
            entities=analysis.detected_entities,
            confidence=analysis.confidence,
            model_used=model.id,
            cost=analysis.cost
        )
```

#### 4.3.3 ChatWork連携

```python
# chatwork-webhook/handlers/multimodal_handler.py

async def handle_message_with_attachment(
    message: ChatWorkMessage,
    brain: SoulkunBrain
) -> str:
    """添付ファイル付きメッセージの処理"""

    attachments = message.attachments
    processed_contents = []

    for attachment in attachments:
        # ファイルタイプに応じた処理
        if attachment.is_image:
            # 画像をダウンロード
            image_data = await chatwork_client.download_file(
                attachment.file_id
            )
            # 画像解析
            result = await image_processor.process(
                image_data=image_data,
                context=brain.context,
                instruction=message.body  # ユーザーの指示
            )
            processed_contents.append({
                'type': 'image',
                'filename': attachment.filename,
                'analysis': result
            })

        elif attachment.is_pdf:
            # PDF処理...
            pass

    # 脳に渡す（元のメッセージ + 解析結果）
    enriched_message = EnrichedMessage(
        original_text=message.body,
        attachments=processed_contents
    )

    return await brain.process(enriched_message)
```

### 4.4 PDF読み込み（M2）

#### 4.4.1 処理フロー

```
PDF受信
    │
    ├─ テキストベースPDF
    │   └─ PyPDF2でテキスト抽出
    │
    └─ スキャンPDF（画像）
        └─ ページごとに画像化 → Vision APIでOCR
    │
    ↓
テキスト + 構造情報（見出し、表）
    │
    ↓
LLMで要約・分析
    │
    ↓
結果を返す + ナレッジDBに保存（オプション）
```

#### 4.4.2 実装

```python
# lib/capabilities/multimodal/pdf_processor.py

class PDFProcessor:
    """PDF処理プロセッサー"""

    MAX_PAGES = 100

    async def process(
        self,
        pdf_data: bytes,
        context: BrainContext,
        save_to_knowledge: bool = False
    ) -> PDFAnalysisResult:
        """PDFを解析"""

        # 1. PDFの種類を判定
        pdf_type = self._detect_pdf_type(pdf_data)

        # 2. テキスト抽出
        if pdf_type == PDFType.TEXT_BASED:
            pages = self._extract_text_pages(pdf_data)
        else:
            # スキャンPDFはOCR
            pages = await self._ocr_pages(pdf_data)

        # 3. 構造解析（見出し、表、図）
        structure = self._analyze_structure(pages)

        # 4. LLMで要約
        summary = await self._summarize(
            pages=pages,
            structure=structure,
            context=context
        )

        # 5. ナレッジDBに保存（オプション）
        if save_to_knowledge:
            await self._save_to_knowledge_db(
                content=pages,
                summary=summary,
                organization_id=context.organization_id
            )

        return PDFAnalysisResult(
            page_count=len(pages),
            full_text='\n'.join(pages),
            summary=summary,
            structure=structure
        )
```

### 4.5 URL読み込み（M3）

#### 4.5.1 ユースケース

```
【記事の要約】
ユーザー: 「このページ要約して」
         https://example.com/article/12345

ソウルくん:
「記事を読んだウル！

📰 タイトル: 2026年のAIトレンド予測

📝 要約（3行）:
1. マルチモーダルAIが主流になる
2. エージェント型AIの実用化が進む
3. コスト効率が大幅に改善

💡 うちの事業との関連:
ソウルくんの方向性と一致してるウル！
特にエージェント機能の強化は重要そう。

全文を読むウル？」
```

#### 4.5.2 実装

```python
# lib/capabilities/multimodal/url_processor.py

class URLProcessor:
    """URL処理プロセッサー"""

    BLOCKED_DOMAINS = ['localhost', '127.0.0.1', '*.internal']
    TIMEOUT_SECONDS = 30

    async def process(
        self,
        url: str,
        context: BrainContext,
        deep_analysis: bool = False
    ) -> URLAnalysisResult:
        """URLの内容を取得・分析"""

        # 1. セキュリティチェック
        if not self._is_safe_url(url):
            raise SecurityError("このURLにはアクセスできないウル")

        # 2. コンテンツ取得
        content = await self._fetch_content(url)

        # 3. HTML解析（本文抽出）
        main_content = self._extract_main_content(content.html)

        # 4. メタ情報抽出
        metadata = self._extract_metadata(content.html)

        # 5. LLMで分析
        if deep_analysis:
            analysis = await self._deep_analyze(
                content=main_content,
                context=context
            )
        else:
            analysis = await self._quick_summarize(main_content)

        # 6. 会社文脈との関連付け
        relevance = await self._find_relevance(
            content=main_content,
            company_knowledge=context.company_knowledge
        )

        return URLAnalysisResult(
            url=url,
            title=metadata.title,
            content=main_content,
            summary=analysis.summary,
            relevance=relevance
        )
```

### 4.6 データベース設計（Phase M）

```sql
-- マルチモーダル処理ログ
CREATE TABLE multimodal_processing_logs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL REFERENCES organizations(id),
    user_id UUID NOT NULL REFERENCES users(id),

    -- 入力情報
    input_type VARCHAR(20) NOT NULL,  -- image, pdf, url, excel, audio
    input_source VARCHAR(50),          -- chatwork_attachment, direct_upload
    file_name VARCHAR(255),
    file_size_bytes INTEGER,

    -- 処理情報
    processor_version VARCHAR(20),
    model_used VARCHAR(100),
    processing_time_ms INTEGER,

    -- 結果
    success BOOLEAN NOT NULL,
    extracted_text TEXT,
    analysis_summary TEXT,
    error_message TEXT,

    -- コスト
    cost_jpy DECIMAL(10, 2),

    -- メタデータ
    metadata JSONB,

    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- インデックス
CREATE INDEX idx_multimodal_logs_org_date
    ON multimodal_processing_logs(organization_id, created_at DESC);
CREATE INDEX idx_multimodal_logs_type
    ON multimodal_processing_logs(input_type);
```

### 4.7 実装スケジュール（Phase M）

| サブフェーズ | 内容 | 工数 | 依存関係 |
|------------|------|:----:|---------|
| M0 | 基盤（ChatWork添付ファイル取得） | 3日 | なし |
| M1 | 画像理解（GPT-4V連携） | 1週間 | M0 |
| M2 | PDF読み込み | 1週間 | M0, M1 |
| M3 | URL読み込み | 1週間 | M0 |
| M4 | Excel/CSV読み込み | 4日 | M0 |
| M5 | 統合テスト + 最適化 | 4日 | M1-M4 |
| **合計** | | **4-5週間** | |

---

## 5. Phase G: Generation（生成能力）

### 5.1 概要

ソウルくんが資料・画像・動画などを自動生成できるようにする。

### 5.2 生成可能なアウトプット

| アウトプット | 使用技術 | 出力先 | 優先度 |
|-------------|---------|--------|:------:|
| **文書（報告書、提案書）** | LLM + Google Docs API | Google Docs / PDF | ★★★ |
| **スプレッドシート** | LLM + Google Sheets API | Google Sheets | ★★☆ |
| **プレゼン資料** | LLM + Google Slides API | Google Slides / PDF | ★★☆ |
| **画像** | DALL-E 3 / Midjourney | PNG / ChatWork送信 | ★★★ |
| **動画** | Runway / Pika | MP4 / Google Drive | ★☆☆ |
| **音声** | ElevenLabs / OpenAI TTS | MP3 | ★☆☆ |

### 5.3 文書生成（G1）

#### 5.3.1 ユースケース

```
【提案書作成】
ユーザー: 「来週のクライアントA向けの提案書作って」

ソウルくん:
「提案書を作成するウル！

📋 使う情報:
・クライアントAの過去の議事録（3件）
・類似案件の提案書テンプレート
・最新のサービス料金表

📝 構成案:
1. エグゼクティブサマリー
2. 課題の整理
3. 提案内容
4. スケジュール
5. お見積り

この構成で進めていいウル？
追加で入れたい内容があれば教えて！」

ユーザー: 「それでOK」

ソウルくん:
「提案書を作成中ウル... 🐺

━━━━━━━━━━━━━━━━━━━━━━
✅ 完成したウル！

📄 提案書_クライアントA_20260127
https://docs.google.com/document/d/xxxxx

📊 推定コスト: ¥45（Claude Opus使用）

確認して、修正点があれば教えてほしいウル！
━━━━━━━━━━━━━━━━━━━━━━」
```

#### 5.3.2 技術仕様

```python
# lib/capabilities/generation/document_generator.py

class DocumentGenerator:
    """文書生成エンジン"""

    async def generate(
        self,
        request: DocumentRequest,
        context: BrainContext
    ) -> DocumentResult:
        """文書を生成"""

        # 1. 関連情報の収集
        relevant_info = await self._gather_relevant_info(
            request=request,
            context=context
        )

        # 2. 構成の生成
        outline = await self._generate_outline(
            request=request,
            relevant_info=relevant_info
        )

        # 3. ユーザー確認（必要な場合）
        if request.require_confirmation:
            return DocumentResult(
                status='pending_confirmation',
                outline=outline
            )

        # 4. 各セクションの生成
        sections = []
        for section in outline.sections:
            content = await self._generate_section(
                section=section,
                relevant_info=relevant_info,
                context=context
            )
            sections.append(content)

        # 5. Google Docsに出力
        doc_url = await self._create_google_doc(
            title=request.title,
            sections=sections,
            template_id=request.template_id
        )

        # 6. 監査ログ
        await self._log_generation(
            request=request,
            result_url=doc_url,
            context=context
        )

        return DocumentResult(
            status='completed',
            document_url=doc_url,
            cost=self._calculate_cost()
        )


@dataclass
class DocumentRequest:
    """文書生成リクエスト"""
    document_type: str  # proposal, report, minutes, etc.
    title: str
    instructions: str
    target_audience: Optional[str] = None
    template_id: Optional[str] = None
    include_sources: List[str] = field(default_factory=list)
    require_confirmation: bool = True
    output_format: str = 'google_docs'  # google_docs, pdf, word
```

### 5.4 画像生成（G2）

#### 5.4.1 ユースケース

```
【イメージ画像の生成】
ユーザー: 「新サービスのイメージ画像作って。
         "AIが人間をサポートする未来的なオフィス"って感じで」

ソウルくん:
「画像を生成中ウル... 🎨

━━━━━━━━━━━━━━━━━━━━━━
✅ 完成したウル！

[画像プレビュー]

📊 コスト: ¥8（DALL-E 3）

気に入らなかったら、修正指示を教えて！
・もっと明るく
・人を増やして
・日本風にして
など」
```

#### 5.4.2 実装

```python
# lib/capabilities/generation/image_generator.py

class ImageGenerator:
    """画像生成エンジン"""

    PROVIDERS = {
        'dalle3': DALLE3Provider,
        'midjourney': MidjourneyProvider,
        'stability': StabilityAIProvider,
    }

    async def generate(
        self,
        request: ImageRequest,
        context: BrainContext
    ) -> ImageResult:
        """画像を生成"""

        # 1. プロンプトの最適化
        optimized_prompt = await self._optimize_prompt(
            user_prompt=request.prompt,
            style=request.style,
            context=context
        )

        # 2. プロバイダー選択
        provider = self._select_provider(
            quality=request.quality,
            style=request.style
        )

        # 3. 生成実行
        image_data = await provider.generate(
            prompt=optimized_prompt,
            size=request.size,
            quality=request.quality
        )

        # 4. 保存（Google Drive or 一時ストレージ）
        image_url = await self._save_image(
            image_data=image_data,
            organization_id=context.organization_id
        )

        # 5. ChatWorkに送信（オプション）
        if request.send_to_chatwork:
            await self._send_to_chatwork(
                image_url=image_url,
                room_id=context.room_id
            )

        return ImageResult(
            image_url=image_url,
            prompt_used=optimized_prompt,
            provider=provider.name,
            cost=provider.calculate_cost()
        )
```

### 5.5 ディープリサーチ（G3）

#### 5.5.1 概要

Perplexityのような深い調査機能。複数ソースから情報を収集し、分析レポートを生成。

#### 5.5.2 ユースケース

```
【競合調査】
ユーザー: 「競合A社について詳しく調べて」

ソウルくん:
「ディープリサーチを開始するウル！🔍

📋 調査項目:
・企業概要
・サービス内容
・最新ニュース
・評判・口コミ
・うちとの比較

⏱ 推定時間: 5-10分
📊 推定コスト: ¥200-400

バックグラウンドで調べておくウル。
完了したら報告するウル！」

[5分後]

ソウルくん:
「━━━━━━━━━━━━━━━━━━━━━━
🔍 ディープリサーチ完了

📊 競合A社 調査レポート

【企業概要】
・設立: 2020年
・従業員: 約50名
・資金調達: シリーズB（10億円）

【サービス】
・主力: AIチャットボット
・価格帯: 月額5-20万円
・強み: カスタマイズ性

【最新動向】
・2026/01: 新機能X発表
・評判: ★★★★☆（4.2/5）

【うちとの比較】
┌────────┬─────────┬─────────┐
│ 項目    │ 競合A   │ ソウル君 │
├────────┼─────────┼─────────┤
│ 価格    │ 高め    │ 中程度   │
│ カスタマイズ │ ◎   │ ○       │
│ 会社理解 │ △      │ ◎       │
└────────┴─────────┴─────────┘

📄 詳細レポート:
https://docs.google.com/document/d/xxxxx

📊 実際のコスト: ¥320
━━━━━━━━━━━━━━━━━━━━━━」
```

#### 5.5.3 実装

```python
# lib/capabilities/generation/deep_research.py

class DeepResearchEngine:
    """ディープリサーチエンジン"""

    async def research(
        self,
        query: str,
        context: BrainContext,
        depth: ResearchDepth = ResearchDepth.STANDARD
    ) -> ResearchResult:
        """深い調査を実行"""

        # 1. 調査計画の生成
        plan = await self._create_research_plan(
            query=query,
            depth=depth
        )

        # 2. 情報収集（並列実行）
        sources = await asyncio.gather(
            self._search_web(plan.web_queries),
            self._search_news(plan.news_queries),
            self._search_academic(plan.academic_queries),
            self._search_internal(plan.internal_queries, context)
        )

        # 3. 情報の統合・分析
        analysis = await self._analyze_sources(
            sources=sources,
            query=query,
            context=context
        )

        # 4. レポート生成
        report = await self._generate_report(
            analysis=analysis,
            format=plan.report_format
        )

        # 5. Google Docsに保存
        doc_url = await self._save_to_google_docs(
            report=report,
            title=f"リサーチレポート: {query}"
        )

        return ResearchResult(
            summary=report.executive_summary,
            full_report_url=doc_url,
            sources_count=len(sources),
            confidence=analysis.confidence,
            cost=self._calculate_total_cost()
        )
```

### 5.6 データベース設計（Phase G）

```sql
-- 生成物ログ
CREATE TABLE generation_logs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL REFERENCES organizations(id),
    user_id UUID NOT NULL REFERENCES users(id),

    -- 生成タイプ
    generation_type VARCHAR(30) NOT NULL,  -- document, image, video, research

    -- リクエスト情報
    request_prompt TEXT NOT NULL,
    request_params JSONB,

    -- 結果
    status VARCHAR(20) NOT NULL,  -- pending, processing, completed, failed
    output_url TEXT,
    output_preview TEXT,  -- 画像のサムネイルURL等

    -- 使用リソース
    models_used JSONB,  -- ["gpt-4o", "dalle-3"]
    sources_used JSONB, -- リサーチの場合のソース

    -- コスト
    cost_jpy DECIMAL(10, 2),

    -- 処理時間
    started_at TIMESTAMP WITH TIME ZONE,
    completed_at TIMESTAMP WITH TIME ZONE,

    -- メタデータ
    metadata JSONB,

    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- インデックス
CREATE INDEX idx_generation_logs_org_date
    ON generation_logs(organization_id, created_at DESC);
CREATE INDEX idx_generation_logs_type_status
    ON generation_logs(generation_type, status);
```

### 5.7 実装スケジュール（Phase G）

| サブフェーズ | 内容 | 工数 | 依存関係 |
|------------|------|:----:|---------|
| G0 | 基盤（生成フレームワーク） | 3日 | なし |
| G1 | 文書生成（Google Docs連携） | 1.5週間 | G0 |
| G2 | 画像生成（DALL-E連携） | 1週間 | G0 |
| G3 | ディープリサーチ | 1.5週間 | G0, Phase M3 |
| G4 | スプレッドシート生成 | 4日 | G0 |
| G5 | 動画生成（Runway連携） | 1週間 | G0, G2 |
| G6 | 統合テスト | 3日 | G1-G5 |
| **合計** | | **5-6週間** | |

---

## 6. Phase AA: Autonomous Agent（自律エージェント）

### 6.1 概要

ソウルくんがバックグラウンドで自律的にタスクを実行できるようにする。

### 6.2 自律実行の種類

| 種類 | 説明 | 例 |
|------|------|-----|
| **バックグラウンドタスク** | 時間のかかる処理を裏で実行 | リサーチ、大量ファイル処理 |
| **スケジュール実行** | 定期的に自動実行 | 日次レポート、週次サマリー |
| **トリガー実行** | 条件を満たしたら自動実行 | 期限超過でリマインド |
| **マルチステップ実行** | 複数ステップを自律的に完了 | 情報収集→分析→報告 |

### 6.3 アーキテクチャ

```
┌─────────────────────────────────────────────────────────────┐
│                    自律エージェント層                        │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                タスクキュー                          │   │
│  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐                  │   │
│  │  │Task1│ │Task2│ │Task3│ │ ... │                  │   │
│  │  └─────┘ └─────┘ └─────┘ └─────┘                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │               エージェントワーカー                    │   │
│  │  ┌──────────────────────────────────────────────┐  │   │
│  │  │ 1. タスク取得                                 │  │   │
│  │  │ 2. 実行計画作成                               │  │   │
│  │  │ 3. ステップ実行（ツール使用）                 │  │   │
│  │  │ 4. 結果検証                                   │  │   │
│  │  │ 5. 次ステップ or 完了                         │  │   │
│  │  └──────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  ツールボックス                      │   │
│  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐         │   │
│  │  │Web  │ │File │ │API  │ │DB   │ │LLM  │         │   │
│  │  │検索 │ │操作 │ │呼出 │ │クエリ│ │呼出 │         │   │
│  │  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘         │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 6.4 バックグラウンドタスク

#### 6.4.1 ユースケース

```
【リサーチのバックグラウンド実行】
ユーザー: 「業界動向を調べておいて」

ソウルくん:
「了解ウル！バックグラウンドで調べておくウル🔍

📋 調査内容:
・業界全体のトレンド
・主要プレイヤーの動向
・規制・法改正の情報

⏱ 推定時間: 15-20分
完了したら報告するウル！」

[15分後、ユーザーが別の作業中に]

ソウルくん:
「カズさん、業界動向の調査が完了したウル！

📊 サマリー:
・AIバックオフィス市場は前年比40%成長
・競合3社が新機能をリリース
・〇〇法の改正が来年施行予定

📄 詳細レポート: [リンク]

今すぐ確認するウル？それとも後でいいウル？」
```

#### 6.4.2 実装

```python
# lib/capabilities/autonomous/task_queue.py

class AutonomousTaskQueue:
    """自律タスクキュー"""

    def __init__(self):
        self.queue = asyncio.Queue()
        self.workers: List[AgentWorker] = []
        self.running_tasks: Dict[str, AutonomousTask] = {}

    async def enqueue(
        self,
        task: AutonomousTask,
        context: BrainContext
    ) -> str:
        """タスクをキューに追加"""

        task_id = str(uuid.uuid4())
        task.id = task_id
        task.status = TaskStatus.QUEUED
        task.context = context

        # DBに記録
        await self._save_task(task)

        # キューに追加
        await self.queue.put(task)

        return task_id

    async def start_workers(self, num_workers: int = 3):
        """ワーカーを起動"""
        for i in range(num_workers):
            worker = AgentWorker(id=f"worker-{i}")
            self.workers.append(worker)
            asyncio.create_task(worker.run(self.queue))


class AgentWorker:
    """エージェントワーカー"""

    async def run(self, queue: asyncio.Queue):
        """タスクを処理し続ける"""
        while True:
            task = await queue.get()
            try:
                await self._execute_task(task)
            except Exception as e:
                await self._handle_error(task, e)
            finally:
                queue.task_done()

    async def _execute_task(self, task: AutonomousTask):
        """タスクを実行"""

        task.status = TaskStatus.RUNNING
        task.started_at = datetime.now()

        # 1. 実行計画を作成
        plan = await self._create_execution_plan(task)

        # 2. ステップを順番に実行
        for step in plan.steps:
            result = await self._execute_step(step, task.context)

            # 結果を検証
            if not result.success:
                if step.retry_on_failure:
                    result = await self._retry_step(step, task.context)
                else:
                    raise StepExecutionError(step, result.error)

            # 中間結果を保存
            task.intermediate_results.append(result)

        # 3. 最終結果を生成
        final_result = await self._generate_final_result(task)

        # 4. ユーザーに通知
        await self._notify_user(task, final_result)

        task.status = TaskStatus.COMPLETED
        task.completed_at = datetime.now()
```

### 6.5 マルチステップ実行

#### 6.5.1 実行計画の例

```
【タスク】「来週の営業会議の準備をして」

【実行計画】
Step 1: 情報収集
  ├─ 先週の議事録を取得
  ├─ 今週のタスク進捗を取得
  ├─ 目標の達成状況を取得
  └─ 関連するナレッジを検索

Step 2: 分析
  ├─ 進捗状況の分析
  ├─ 課題の抽出
  └─ 議題候補のリストアップ

Step 3: 資料作成
  ├─ アジェンダ案を作成
  ├─ 進捗サマリーを作成
  └─ Google Slidesに出力

Step 4: 報告
  └─ ユーザーに完了報告 + 資料リンク
```

### 6.6 ツール定義

```python
# lib/capabilities/autonomous/tools.py

AVAILABLE_TOOLS = {
    'web_search': {
        'name': 'Web検索',
        'description': 'インターネットで情報を検索',
        'parameters': {
            'query': {'type': 'string', 'required': True},
            'num_results': {'type': 'integer', 'default': 10}
        },
        'handler': web_search_handler
    },

    'read_document': {
        'name': 'ドキュメント読み込み',
        'description': '社内ドキュメントを読み込む',
        'parameters': {
            'document_id': {'type': 'string', 'required': True}
        },
        'handler': read_document_handler
    },

    'query_database': {
        'name': 'データベースクエリ',
        'description': '社内データベースを検索',
        'parameters': {
            'query_type': {'type': 'string', 'required': True},
            'filters': {'type': 'object'}
        },
        'handler': query_database_handler
    },

    'create_document': {
        'name': 'ドキュメント作成',
        'description': 'Google Docsでドキュメントを作成',
        'parameters': {
            'title': {'type': 'string', 'required': True},
            'content': {'type': 'string', 'required': True}
        },
        'handler': create_document_handler
    },

    'send_message': {
        'name': 'メッセージ送信',
        'description': 'ChatWorkでメッセージを送信',
        'parameters': {
            'room_id': {'type': 'string', 'required': True},
            'message': {'type': 'string', 'required': True}
        },
        'handler': send_message_handler
    },

    'call_llm': {
        'name': 'LLM呼び出し',
        'description': 'LLMで分析・生成を実行',
        'parameters': {
            'prompt': {'type': 'string', 'required': True},
            'model_tier': {'type': 'integer', 'default': 2}
        },
        'handler': call_llm_handler
    }
}
```

### 6.7 データベース設計（Phase AA）

```sql
-- 自律タスク
CREATE TABLE autonomous_tasks (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL REFERENCES organizations(id),
    user_id UUID NOT NULL REFERENCES users(id),

    -- タスク情報
    task_type VARCHAR(50) NOT NULL,  -- research, preparation, monitoring
    description TEXT NOT NULL,
    priority INTEGER DEFAULT 5,

    -- 実行計画
    execution_plan JSONB,

    -- ステータス
    status VARCHAR(20) NOT NULL,  -- queued, running, completed, failed, cancelled
    progress_percent INTEGER DEFAULT 0,

    -- 結果
    intermediate_results JSONB,
    final_result JSONB,
    error_message TEXT,

    -- タイミング
    scheduled_at TIMESTAMP WITH TIME ZONE,
    started_at TIMESTAMP WITH TIME ZONE,
    completed_at TIMESTAMP WITH TIME ZONE,

    -- コスト
    estimated_cost_jpy DECIMAL(10, 2),
    actual_cost_jpy DECIMAL(10, 2),

    -- メタデータ
    metadata JSONB,

    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 実行ログ（ステップごと）
CREATE TABLE autonomous_task_steps (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    task_id UUID NOT NULL REFERENCES autonomous_tasks(id),

    step_number INTEGER NOT NULL,
    step_name VARCHAR(100) NOT NULL,
    tool_used VARCHAR(50),

    -- 入出力
    input_params JSONB,
    output_result JSONB,

    -- ステータス
    status VARCHAR(20) NOT NULL,
    error_message TEXT,

    -- タイミング
    started_at TIMESTAMP WITH TIME ZONE,
    completed_at TIMESTAMP WITH TIME ZONE,

    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- インデックス
CREATE INDEX idx_autonomous_tasks_org_status
    ON autonomous_tasks(organization_id, status);
CREATE INDEX idx_autonomous_tasks_scheduled
    ON autonomous_tasks(scheduled_at) WHERE status = 'queued';
```

### 6.8 実装スケジュール（Phase AA）

| サブフェーズ | 内容 | 工数 | 依存関係 |
|------------|------|:----:|---------|
| AA0 | タスクキュー基盤 | 1週間 | なし |
| AA1 | ワーカーシステム | 1週間 | AA0 |
| AA2 | ツール定義・実装 | 1.5週間 | AA1 |
| AA3 | 実行計画生成（LLM） | 1週間 | AA2 |
| AA4 | 進捗報告・通知 | 4日 | AA1 |
| AA5 | スケジュール実行 | 4日 | AA0 |
| AA6 | エラーハンドリング・リトライ | 4日 | AA1-AA4 |
| AA7 | 統合テスト | 1週間 | AA1-AA6 |
| **合計** | | **6-8週間** | |

---

## 7. Phase F: Feedback（CEOフィードバック）

### 7.1 概要

ソウルくんが事実に基づいてCEOにフィードバックを提供し、意思決定を支援する。

### 7.2 フィードバックの種類

| 種類 | 頻度 | 内容 |
|------|------|------|
| **デイリーダイジェスト** | 毎朝8:00 | 今日注目すべきこと |
| **ウィークリーレビュー** | 毎週月曜9:00 | 先週の振り返り + 今週の注目点 |
| **マンスリーインサイト** | 毎月1日9:00 | 月次分析 + トレンド + 提案 |
| **リアルタイムアラート** | 随時 | 重要な変化を即座に通知 |
| **オンデマンド分析** | 聞いた時 | 「最近どう？」で深掘り |

### 7.3 フィードバックの原則

| # | 原則 | 説明 |
|---|------|------|
| 1 | **事実ファースト** | 「〜だと思います」ではなく「〜というデータがあります」 |
| 2 | **数字で語る** | 感覚ではなく、具体的な数値変化を提示 |
| 3 | **比較を入れる** | 先週比、先月比、過去パターンとの比較 |
| 4 | **仮説は仮説と明示** | 「〜かもしれません」と断定しない |
| 5 | **アクション提案** | 問題提起だけでなく「こうしては？」まで |
| 6 | **ポジティブも伝える** | 問題だけでなく、良いことも報告 |

### 7.4 アーキテクチャ

```
┌─────────────────────────────────────────────────────────────┐
│                  CEOフィードバックシステム                   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              ファクト収集エンジン                    │   │
│  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐         │   │
│  │  │タスク│ │目標 │ │会話 │ │会議 │ │感情 │         │   │
│  │  │メトリクス│ │進捗 │ │パターン│ │記録 │ │変化 │     │   │
│  │  └──┬──┘ └──┬──┘ └──┬──┘ └──┬──┘ └──┬──┘         │   │
│  │     └───────┴───────┴───────┴───────┘               │   │
│  │                       ↓                              │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  分析エンジン                        │   │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐   │   │
│  │  │ トレンド分析 │ │ 異常検知    │ │パターンマッチ│   │   │
│  │  │ (時系列比較) │ │ (閾値超過)  │ │ (過去類似)  │   │   │
│  │  └─────────────┘ └─────────────┘ └─────────────┘   │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              フィードバック生成エンジン              │   │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐   │   │
│  │  │ 事実の整理  │→│ 仮説の生成  │→│ 提案の作成  │   │   │
│  │  └─────────────┘ └─────────────┘ └─────────────┘   │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                   配信システム                       │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐              │   │
│  │  │スケジュール│ │リアルタイム│ │オンデマンド│          │   │
│  │  │ 配信     │ │ アラート │ │ 応答    │              │   │
│  │  └─────────┘ └─────────┘ └─────────┘              │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 7.5 ファクト収集

```python
# lib/capabilities/feedback/fact_collector.py

class FactCollector:
    """ファクト収集エンジン"""

    async def collect_daily_facts(
        self,
        organization_id: UUID,
        date: date
    ) -> DailyFacts:
        """日次ファクトを収集"""

        facts = DailyFacts(date=date)

        # タスク関連
        facts.tasks = await self._collect_task_facts(organization_id, date)
        # - 完了数、未完了数
        # - 期限超過数
        # - 新規作成数
        # - 担当者別内訳

        # 目標関連
        facts.goals = await self._collect_goal_facts(organization_id, date)
        # - 進捗率の変化
        # - 停滞している目標
        # - 達成された目標

        # コミュニケーション関連
        facts.communication = await self._collect_communication_facts(
            organization_id, date
        )
        # - メッセージ数
        # - 返信速度
        # - 質問の傾向

        # 感情関連（Phase 2A4の結果を利用）
        facts.emotions = await self._collect_emotion_facts(organization_id, date)
        # - 感情変化のあったユーザー
        # - 全体的なトーン

        return facts

    async def collect_weekly_facts(
        self,
        organization_id: UUID,
        week_start: date
    ) -> WeeklyFacts:
        """週次ファクトを収集"""
        # 日次ファクトを集約 + 週単位の分析
        pass

    async def collect_monthly_facts(
        self,
        organization_id: UUID,
        month: date
    ) -> MonthlyFacts:
        """月次ファクトを収集"""
        # 週次ファクトを集約 + 月単位の分析
        pass
```

### 7.6 分析エンジン

```python
# lib/capabilities/feedback/analyzer.py

class FeedbackAnalyzer:
    """フィードバック分析エンジン"""

    async def analyze(
        self,
        facts: Facts,
        context: BrainContext
    ) -> AnalysisResult:
        """ファクトを分析"""

        insights = []

        # 1. トレンド分析（前期比較）
        trends = await self._analyze_trends(facts)
        for trend in trends:
            if trend.is_significant:
                insights.append(Insight(
                    type='trend',
                    title=trend.title,
                    data=trend.data,
                    significance=trend.significance
                ))

        # 2. 異常検知（閾値ベース）
        anomalies = await self._detect_anomalies(facts)
        for anomaly in anomalies:
            insights.append(Insight(
                type='anomaly',
                title=anomaly.title,
                data=anomaly.data,
                severity=anomaly.severity
            ))

        # 3. パターンマッチング（過去の類似状況）
        patterns = await self._match_patterns(facts, context)
        for pattern in patterns:
            insights.append(Insight(
                type='pattern',
                title=pattern.title,
                historical_context=pattern.past_occurrence,
                prediction=pattern.likely_outcome
            ))

        # 4. インサイトの優先順位付け
        prioritized_insights = self._prioritize(insights)

        # 5. 仮説と提案の生成
        hypotheses = await self._generate_hypotheses(prioritized_insights)
        recommendations = await self._generate_recommendations(
            insights=prioritized_insights,
            hypotheses=hypotheses
        )

        return AnalysisResult(
            insights=prioritized_insights,
            hypotheses=hypotheses,
            recommendations=recommendations
        )
```

### 7.7 フィードバック生成

```python
# lib/capabilities/feedback/generator.py

class FeedbackGenerator:
    """フィードバック生成エンジン"""

    async def generate_daily_digest(
        self,
        analysis: AnalysisResult,
        context: BrainContext
    ) -> DailyDigest:
        """デイリーダイジェストを生成"""

        # LLMでフィードバック文章を生成
        prompt = self._build_daily_prompt(analysis, context)

        feedback_text = await self.llm.generate(
            prompt=prompt,
            style='concise_business',
            persona='supportive_advisor'
        )

        return DailyDigest(
            date=date.today(),
            summary=feedback_text,
            key_points=analysis.top_insights(3),
            action_items=analysis.recommendations[:3],
            metrics=self._extract_key_metrics(analysis)
        )

    async def generate_realtime_alert(
        self,
        anomaly: Anomaly,
        context: BrainContext
    ) -> RealtimeAlert:
        """リアルタイムアラートを生成"""

        # アラートの緊急度を判定
        urgency = self._assess_urgency(anomaly)

        # アラート文章を生成
        alert_text = await self._generate_alert_text(
            anomaly=anomaly,
            urgency=urgency,
            context=context
        )

        return RealtimeAlert(
            urgency=urgency,
            title=anomaly.title,
            message=alert_text,
            data=anomaly.data,
            suggested_action=anomaly.recommended_action
        )
```

### 7.8 フィードバックテンプレート

#### 7.8.1 デイリーダイジェスト

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 {date} カズさんへのフィードバック
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【今日注目してほしいこと】

{for insight in top_insights}
{insight.icon} {insight.title}
   → {insight.action_suggestion}

{end for}

【数字で見る昨日】
┌────────────────────────────────┐
│ タスク完了: {task_completed}件          │
│ 期限超過:   {task_overdue}件            │
│ 目標進捗:   {goal_progress}%            │
└────────────────────────────────┘

【カズさんの今日の予定との関連】
{schedule_relevance}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### 7.8.2 ウィークリーレビュー

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 週次レビュー（{week_start}〜{week_end}）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【サマリー】
{executive_summary}

【数字で見る先週】
┌────────────────────────────────┐
│ タスク完了率: {completion_rate}% ({change})   │
│ 期限超過数:   {overdue_count}件 ({change})    │
│ 目標進捗平均: {goal_avg}% ({change})          │
│ 会議時間合計: {meeting_hours}h ({change})     │
└────────────────────────────────┘

【良かったこと】
{for positive in positives}
✅ {positive}
{end for}

【気になること】
{for concern in concerns}
⚠️ {concern.title}
   → {concern.suggestion}
{end for}

【今週の提案】
{for recommendation in recommendations}
{index}. {recommendation}
{end for}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 7.9 データベース設計（Phase F）

```sql
-- フィードバック配信ログ
CREATE TABLE feedback_deliveries (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL REFERENCES organizations(id),
    recipient_user_id UUID NOT NULL REFERENCES users(id),

    -- フィードバックタイプ
    feedback_type VARCHAR(30) NOT NULL,  -- daily, weekly, monthly, realtime, ondemand

    -- 内容
    title VARCHAR(200),
    content TEXT NOT NULL,
    insights JSONB,  -- 含まれるインサイト
    recommendations JSONB,  -- 含まれる提案

    -- 配信情報
    delivery_channel VARCHAR(20) NOT NULL,  -- chatwork, email
    delivered_at TIMESTAMP WITH TIME ZONE,
    read_at TIMESTAMP WITH TIME ZONE,

    -- フィードバックへの反応
    user_reaction VARCHAR(20),  -- helpful, not_helpful, null
    user_comment TEXT,

    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- インサイトログ（学習用）
CREATE TABLE feedback_insights (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL REFERENCES organizations(id),

    -- インサイト内容
    insight_type VARCHAR(30) NOT NULL,  -- trend, anomaly, pattern
    category VARCHAR(50),  -- task, goal, communication, emotion
    title VARCHAR(200) NOT NULL,
    description TEXT,

    -- データ
    data_points JSONB,
    significance_score DECIMAL(3, 2),  -- 0.00 - 1.00

    -- 検出日時
    detected_at TIMESTAMP WITH TIME ZONE NOT NULL,

    -- 配信されたか
    was_delivered BOOLEAN DEFAULT false,
    delivery_id UUID REFERENCES feedback_deliveries(id),

    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- フィードバック設定
CREATE TABLE feedback_settings (
    organization_id UUID PRIMARY KEY REFERENCES organizations(id),

    -- スケジュール設定
    daily_digest_enabled BOOLEAN DEFAULT true,
    daily_digest_time TIME DEFAULT '08:00',
    weekly_review_enabled BOOLEAN DEFAULT true,
    weekly_review_day INTEGER DEFAULT 1,  -- 1=Monday
    weekly_review_time TIME DEFAULT '09:00',
    monthly_insight_enabled BOOLEAN DEFAULT true,

    -- アラート設定
    realtime_alert_enabled BOOLEAN DEFAULT true,
    alert_emotion_change BOOLEAN DEFAULT true,
    alert_task_overdue_days INTEGER DEFAULT 3,
    alert_goal_stagnation_days INTEGER DEFAULT 14,

    -- 配信先
    delivery_channel VARCHAR(20) DEFAULT 'chatwork',
    recipient_user_id UUID REFERENCES users(id),

    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- インデックス
CREATE INDEX idx_feedback_deliveries_org_date
    ON feedback_deliveries(organization_id, created_at DESC);
CREATE INDEX idx_feedback_insights_org_detected
    ON feedback_insights(organization_id, detected_at DESC);
```

### 7.10 実装スケジュール（Phase F）

| サブフェーズ | 内容 | 工数 | 依存関係 |
|------------|------|:----:|---------|
| F0 | ファクト収集基盤 | 1週間 | なし |
| F1 | 分析エンジン（トレンド・異常検知） | 1.5週間 | F0 |
| F2 | フィードバック生成（LLM） | 1週間 | F1 |
| F3 | 配信システム（スケジュール） | 3日 | F2 |
| F4 | リアルタイムアラート | 3日 | F1, F2 |
| F5 | オンデマンド分析 | 3日 | F1, F2 |
| F6 | 設定画面 | 2日 | F3-F5 |
| F7 | 統合テスト | 3日 | F0-F6 |
| **合計** | | **4-5週間** | |

---

## 8. スマートモデル管理システム

### 8.1 概要

常に最新のAIモデルを最適なコストで利用するための管理システム。

### 8.2 モデルティア制

| ティア | 用途 | モデル例 | コスト目安 |
|:------:|------|---------|-----------|
| **Tier 1** | 軽いタスク | GPT-4o-mini, Haiku, Flash | ¥0.1-1/回 |
| **Tier 2** | 標準タスク | GPT-4o, Sonnet, Pro | ¥1-10/回 |
| **Tier 3** | 重要タスク | Opus, o1, Ultra | ¥10-100/回 |

### 8.3 自動モデル選択ルール

```python
# lib/capabilities/model_orchestrator/selector.py

class ModelSelector:
    """モデル選択エンジン"""

    # ティア判定キーワード
    TIER3_KEYWORDS = [
        'ちゃんと', 'しっかり', '本気で', '重要な',
        '提案書', '企画書', 'クライアント向け', '正式な'
    ]

    TIER1_KEYWORDS = [
        'ざっくり', '簡単に', 'とりあえず', 'さくっと',
        '概要だけ', '軽く'
    ]

    # タスクタイプ別デフォルトティア
    TASK_TYPE_TIERS = {
        'simple_qa': 1,
        'reminder': 1,
        'task_list': 1,
        'document_generation': 3,
        'deep_research': 3,
        'analysis': 2,
        'chat': 2,
        'image_generation': 2,
    }

    async def select_model(
        self,
        task_type: str,
        user_message: str,
        context: BrainContext
    ) -> ModelSelection:
        """最適なモデルを選択"""

        # 1. ベースティアを決定
        base_tier = self.TASK_TYPE_TIERS.get(task_type, 2)

        # 2. ユーザーの言い方でティアを調整
        adjusted_tier = self._adjust_tier_by_keywords(
            base_tier, user_message
        )

        # 3. コスト設定を確認
        settings = await self._get_org_settings(context.organization_id)

        # 4. ティアに応じたモデルを選択
        model = await self._select_model_for_tier(
            tier=adjusted_tier,
            task_type=task_type,
            preferred_provider=settings.preferred_provider
        )

        # 5. コスト見積もり
        estimated_cost = self._estimate_cost(model, task_type)

        return ModelSelection(
            model=model,
            tier=adjusted_tier,
            estimated_cost=estimated_cost,
            requires_confirmation=estimated_cost > settings.cost_confirm_threshold
        )
```

### 8.4 コスト相談モード

```python
# lib/capabilities/model_orchestrator/cost_manager.py

class CostManager:
    """コスト管理マネージャー"""

    # デフォルト閾値
    DEFAULT_THRESHOLDS = {
        'auto_execute': 100,       # ¥100未満は自動実行
        'report_after': 500,       # ¥500未満は実行後報告
        'confirm_before': 2000,    # ¥2000未満は事前確認
        'require_approval': 2000   # ¥2000以上は必須確認
    }

    async def check_cost(
        self,
        estimated_cost: Decimal,
        context: BrainContext
    ) -> CostCheckResult:
        """コストをチェックして適切なアクションを決定"""

        settings = await self._get_settings(context.organization_id)
        thresholds = settings.thresholds or self.DEFAULT_THRESHOLDS

        if estimated_cost < thresholds['auto_execute']:
            return CostCheckResult(
                action=CostAction.AUTO_EXECUTE,
                message=None
            )

        elif estimated_cost < thresholds['report_after']:
            return CostCheckResult(
                action=CostAction.EXECUTE_AND_REPORT,
                message=self._build_report_message(estimated_cost)
            )

        elif estimated_cost < thresholds['require_approval']:
            return CostCheckResult(
                action=CostAction.CONFIRM_BEFORE,
                message=self._build_confirmation_message(
                    estimated_cost,
                    alternatives=await self._get_cheaper_alternatives()
                )
            )

        else:
            return CostCheckResult(
                action=CostAction.REQUIRE_APPROVAL,
                message=self._build_approval_message(estimated_cost)
            )
```

### 8.5 モデルレジストリ自動更新

```python
# lib/capabilities/model_orchestrator/registry.py

class ModelRegistry:
    """モデルレジストリ"""

    async def check_for_updates(self):
        """新しいモデルがないかチェック"""

        updates = []

        # OpenAI
        openai_models = await self._fetch_openai_models()
        updates.extend(self._detect_new_models('openai', openai_models))

        # Anthropic
        anthropic_models = await self._fetch_anthropic_models()
        updates.extend(self._detect_new_models('anthropic', anthropic_models))

        # Google
        google_models = await self._fetch_google_models()
        updates.extend(self._detect_new_models('google', google_models))

        # 新モデルがあれば通知
        if updates:
            await self._notify_admin(updates)
            await self._save_new_models(updates)

        return updates

    async def get_latest_model(
        self,
        provider: str,
        capability: str
    ) -> Model:
        """指定プロバイダーの最新モデルを取得"""

        return await self.db.query(
            """
            SELECT * FROM ai_model_registry
            WHERE provider = :provider
              AND :capability = ANY(capabilities)
              AND is_active = true
            ORDER BY release_date DESC
            LIMIT 1
            """,
            {'provider': provider, 'capability': capability}
        )
```

### 8.6 データベース設計（モデル管理）

```sql
-- モデルレジストリ
CREATE TABLE ai_model_registry (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),

    -- プロバイダー情報
    provider VARCHAR(50) NOT NULL,  -- openai, anthropic, google, stability
    model_id VARCHAR(100) NOT NULL UNIQUE,  -- gpt-4o-2024-11-20
    model_name VARCHAR(100) NOT NULL,  -- GPT-4o
    model_family VARCHAR(50),  -- gpt-4, claude-3, gemini

    -- 能力
    capabilities JSONB NOT NULL,  -- ["text", "vision", "code", "image_gen"]
    context_window INTEGER,
    max_output_tokens INTEGER,

    -- ティア
    tier INTEGER NOT NULL,  -- 1, 2, 3

    -- コスト（1000トークンあたり、円）
    cost_per_1k_input DECIMAL(10, 4),
    cost_per_1k_output DECIMAL(10, 4),
    cost_per_image DECIMAL(10, 2),  -- 画像生成の場合

    -- メタ情報
    release_date DATE,
    is_latest BOOLEAN DEFAULT false,
    is_active BOOLEAN DEFAULT true,
    deprecation_date DATE,

    -- 性能メトリクス（実測値）
    avg_latency_ms INTEGER,
    quality_score DECIMAL(3, 2),  -- 内部評価スコア

    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 利用ログ
CREATE TABLE ai_usage_logs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL REFERENCES organizations(id),
    user_id UUID REFERENCES users(id),

    -- 使用モデル
    model_id UUID NOT NULL REFERENCES ai_model_registry(id),

    -- タスク情報
    task_type VARCHAR(50) NOT NULL,
    task_description TEXT,

    -- トークン使用量
    input_tokens INTEGER,
    output_tokens INTEGER,

    -- コスト
    cost_jpy DECIMAL(10, 4) NOT NULL,

    -- ティア情報
    tier_used INTEGER NOT NULL,
    tier_recommended INTEGER,  -- 推奨されたティア（ダウングレードした場合に記録）

    -- パフォーマンス
    latency_ms INTEGER,

    -- 結果
    success BOOLEAN NOT NULL,
    error_message TEXT,

    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 組織別AI設定
CREATE TABLE ai_organization_settings (
    organization_id UUID PRIMARY KEY REFERENCES organizations(id),

    -- モデル選択ポリシー
    model_policy VARCHAR(20) DEFAULT 'balanced',  -- best, balanced, economical
    preferred_provider VARCHAR(50),  -- 優先プロバイダー

    -- コスト設定
    cost_confirm_threshold INTEGER DEFAULT 500,  -- 確認が必要な金額
    monthly_budget INTEGER,  -- 月次予算

    -- 通知設定
    notify_new_models BOOLEAN DEFAULT true,
    send_monthly_report BOOLEAN DEFAULT true,

    -- フォールバック設定
    fallback_enabled BOOLEAN DEFAULT true,
    fallback_order JSONB,  -- ["anthropic", "openai", "google"]

    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 月次コストサマリー
CREATE TABLE ai_monthly_cost_summary (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL REFERENCES organizations(id),
    year_month DATE NOT NULL,  -- 2026-01-01

    -- 総コスト
    total_cost_jpy DECIMAL(12, 2) NOT NULL,

    -- プロバイダー別
    cost_by_provider JSONB,  -- {"openai": 5000, "anthropic": 3000}

    -- タスクタイプ別
    cost_by_task_type JSONB,  -- {"research": 2000, "document": 1500}

    -- ティア別
    cost_by_tier JSONB,  -- {"1": 500, "2": 3000, "3": 4500}

    -- 使用量
    total_requests INTEGER,
    total_tokens INTEGER,

    -- 最適化の余地
    potential_savings_jpy DECIMAL(12, 2),  -- ティアダウンで節約できた額

    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- インデックス
CREATE INDEX idx_ai_model_registry_provider_latest
    ON ai_model_registry(provider, is_latest) WHERE is_active = true;
CREATE INDEX idx_ai_usage_logs_org_date
    ON ai_usage_logs(organization_id, created_at DESC);
CREATE INDEX idx_ai_monthly_cost_org_month
    ON ai_monthly_cost_summary(organization_id, year_month DESC);
```

### 8.7 実装スケジュール（モデル管理）

| サブフェーズ | 内容 | 工数 | 依存関係 |
|------------|------|:----:|---------|
| SM0 | モデルレジストリ基盤 | 4日 | なし |
| SM1 | モデル自動選択ロジック | 1週間 | SM0 |
| SM2 | コスト管理・相談モード | 4日 | SM1 |
| SM3 | 自動更新チェック | 3日 | SM0 |
| SM4 | 利用ログ・月次レポート | 3日 | SM1 |
| SM5 | フォールバック機構 | 3日 | SM0, SM1 |
| SM6 | 設定画面 | 2日 | SM1-SM5 |
| **合計** | | **3-4週間** | |

---

## 9. データベース設計

### 9.1 新規テーブル一覧

| テーブル名 | Phase | 用途 |
|-----------|-------|------|
| `multimodal_processing_logs` | M | マルチモーダル処理ログ |
| `generation_logs` | G | 生成物ログ |
| `autonomous_tasks` | AA | 自律タスク |
| `autonomous_task_steps` | AA | タスク実行ステップ |
| `feedback_deliveries` | F | フィードバック配信ログ |
| `feedback_insights` | F | インサイトログ |
| `feedback_settings` | F | フィードバック設定 |
| `ai_model_registry` | SM | モデルレジストリ |
| `ai_usage_logs` | SM | AI利用ログ |
| `ai_organization_settings` | SM | 組織別AI設定 |
| `ai_monthly_cost_summary` | SM | 月次コストサマリー |

### 9.2 ER図（概要）

```
┌─────────────────┐     ┌─────────────────┐
│  organizations  │     │     users       │
└────────┬────────┘     └────────┬────────┘
         │                       │
         │    ┌──────────────────┤
         │    │                  │
         ▼    ▼                  ▼
┌─────────────────┐     ┌─────────────────┐
│multimodal_logs  │     │ generation_logs │
└─────────────────┘     └─────────────────┘
         │
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│autonomous_tasks │────▶│autonomous_steps │
└─────────────────┘     └─────────────────┘
         │
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│feedback_deliver │     │feedback_insights│
└─────────────────┘     └─────────────────┘
         │
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│ai_usage_logs    │────▶│ai_model_registry│
└─────────────────┘     └─────────────────┘
```

### 9.3 マイグレーションファイル

```sql
-- migrations/next_generation_capabilities.sql

-- ============================================
-- Phase M: Multimodal
-- ============================================

CREATE TABLE multimodal_processing_logs (
    -- ... (詳細は4.6参照)
);

-- ============================================
-- Phase G: Generation
-- ============================================

CREATE TABLE generation_logs (
    -- ... (詳細は5.6参照)
);

-- ============================================
-- Phase AA: Autonomous Agent
-- ============================================

CREATE TABLE autonomous_tasks (
    -- ... (詳細は6.7参照)
);

CREATE TABLE autonomous_task_steps (
    -- ... (詳細は6.7参照)
);

-- ============================================
-- Phase F: Feedback
-- ============================================

CREATE TABLE feedback_deliveries (
    -- ... (詳細は7.9参照)
);

CREATE TABLE feedback_insights (
    -- ... (詳細は7.9参照)
);

CREATE TABLE feedback_settings (
    -- ... (詳細は7.9参照)
);

-- ============================================
-- Smart Model Management
-- ============================================

CREATE TABLE ai_model_registry (
    -- ... (詳細は8.6参照)
);

CREATE TABLE ai_usage_logs (
    -- ... (詳細は8.6参照)
);

CREATE TABLE ai_organization_settings (
    -- ... (詳細は8.6参照)
);

CREATE TABLE ai_monthly_cost_summary (
    -- ... (詳細は8.6参照)
);
```

---

## 10. 実装ロードマップ

### 10.1 全体スケジュール

```
2026年 Q1（2-3月）
├─ スマートモデル管理システム（3-4週間）
├─ Phase M: Multimodal（4-5週間）
└─ Phase F: Feedback（開始）

2026年 Q2（4-6月）
├─ Phase F: Feedback（完了）
├─ Phase G: Generation（5-6週間）
└─ Phase AA: Autonomous Agent（開始）

2026年 Q3（7-9月）
├─ Phase AA: Autonomous Agent（完了）
└─ 統合テスト・最適化

【並行して】脳フェーズ 2F〜2O
```

### 10.2 詳細スケジュール

| 週 | Phase | タスク |
|----|-------|--------|
| Week 1-2 | SM | モデル管理基盤 |
| Week 3-4 | SM | モデル選択・コスト管理 |
| Week 5-6 | M | マルチモーダル基盤・画像理解 |
| Week 7-8 | M | PDF・URL読み込み |
| Week 9 | M | 統合・テスト |
| Week 10-11 | F | ファクト収集・分析エンジン |
| Week 12-13 | F | フィードバック生成・配信 |
| Week 14-15 | G | 文書生成・Google連携 |
| Week 16-17 | G | 画像生成・ディープリサーチ |
| Week 18-19 | G | 動画生成・統合 |
| Week 20-23 | AA | タスクキュー・ワーカー |
| Week 24-26 | AA | ツール連携・マルチステップ |
| Week 27-28 | 全体 | 統合テスト・最適化 |

### 10.3 依存関係図

```
スマートモデル管理 ──────────────────────────┐
        │                                    │
        ▼                                    │
   Phase M ──────────┬───────────────────────┤
   (Multimodal)      │                       │
        │            │                       │
        ▼            ▼                       │
   Phase G      Phase F                      │
   (Generation) (Feedback)                   │
        │            │                       │
        └─────┬──────┘                       │
              │                              │
              ▼                              │
         Phase AA  ◀─────────────────────────┘
         (Autonomous Agent)
```

---

## 11. リスクと対策

### 11.1 技術リスク

| リスク | 影響 | 確率 | 対策 |
|--------|:----:|:----:|------|
| API障害（OpenAI等） | 高 | 中 | フォールバック機構、複数プロバイダー |
| コスト超過 | 高 | 中 | 相談モード、月次予算、アラート |
| 処理時間超過 | 中 | 中 | タイムアウト設定、非同期処理 |
| データ漏洩 | 高 | 低 | organization_id必須、監査ログ |

### 11.2 ビジネスリスク

| リスク | 影響 | 確率 | 対策 |
|--------|:----:|:----:|------|
| 利用されない | 高 | 中 | 段階的リリース、フィードバック収集 |
| 誤った情報生成 | 高 | 中 | 確認フロー、ソース明示 |
| コスト高すぎ | 中 | 中 | ティア制、予算設定 |

### 11.3 対策詳細

**フォールバック機構:**
```
Primary: Claude Opus 4.5
    ↓ 失敗
Fallback 1: GPT-4o
    ↓ 失敗
Fallback 2: Gemini Ultra
    ↓ 全失敗
エラー通知 + リトライ案内
```

**コスト管理:**
```
・デフォルト予算: ¥30,000/月
・¥500以上は事前確認
・月末に近づいたらアラート
・予算超過で自動的にTier 1制限
```

---

## 12. 統合アプリケーション

### 12.1 App1: 議事録自動生成（基本版）

**ステータス:** ✅ 実装完了（2026-01-27）

Phase M2（音声入力）とPhase G1（文書生成）を統合した議事録自動生成アプリ。
詳細は `lib/capabilities/apps/meeting_minutes/` を参照。

### 12.2 App2: 会議議事録自動化システム v2（Zoom連携）

**ステータス:** 📋 設計完了、実装待ち

#### 12.2.1 概要

Zoom録画からGoogleカレンダー情報を照合し、議事録を自動生成してChatWorkに配信。
タスクも自動抽出して担当者に割り当てる完全自動化システム。

#### 12.2.2 全体フロー

```
┌─────────────────────────────────────────────────────────────────┐
│                  会議議事録自動化システム v2                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  【トリガー】                                                    │
│  Zoom録画完了 ──▶ Webhook受信                                   │
│                                                                 │
│  【照合】                                                        │
│  ┌─────────────┐      ┌─────────────────┐                      │
│  │ Zoom録画情報 │ ◀──▶ │ Googleカレンダー │                      │
│  │ ・開始時刻   │      │ ・会議タイトル   │                      │
│  │ ・参加者    │      │ ・参加者        │                      │
│  │ ・ホストID  │      │ ・説明欄        │                      │
│  └─────────────┘      └─────────────────┘                      │
│         │                    │                                  │
│         └────────┬───────────┘                                  │
│                  ▼                                              │
│  【処理】                                                        │
│  1. 動画ダウンロード                                            │
│  2. 音声抽出                                                    │
│  3. 文字起こし（Whisper）                                       │
│  4. 固定プロンプトで要約生成（詳細は12.2.4参照）                 │
│  5. タスク抽出 + 担当者特定                                      │
│  6. 脳への統合（記憶・学習・知識グラフ）                         │
│                                                                 │
│  【出力】                                                        │
│  ChatWork該当グループ ──▶ 議事録テキスト + 動画URL               │
│  ChatWorkタスク ──▶ 担当者に自動追加（期限1週間）                │
│                                                                 │
│  【活用】                                                        │
│  ソウルくんが議事録を記憶し、今後の対話・判断に活用              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 12.2.3 ChatWorkグループ特定ロジック

```
優先度1: マッピングテーブルで自動判定
  会議名に含むキーワード    →    ChatWorkルーム
  ─────────────────────────────────────────────
  「営業定例」            →    営業部ルーム
  「開発MTG」             →    開発チームルーム
  「1on1」「面談」        →    該当者のDMルーム
  「全社」「全体」        →    全社ルーム

優先度2: カレンダー説明欄に「CW:12345678」があれば使う

優先度3: 不明なら管理部に確認メッセージ
         「この会議の議事録、どこに送る？」
```

#### 12.2.4 固定プロンプト（議事録生成用）

```
上記の動画を内容が細かく具体的に理解できるように要約を作成してください。
大事なことを記録する際になぜそうなったのかがわかる背景も省かず詳細に記載してください。
その中で発生したタスクを全て洗い出してください。

さらに例え話や実例、ワークテーマ、数字に関する話などは省かずに出たもの全て
要約の中に書き出してください。

そして、その要約はあたかもあなたが私に講義として教えてくれてるかのような
書き方で要約を作成してください。

要約はLINEなどのチャットでコピペできるようにテキストで書き出してください。
要約は「■主題（00:00〜）」のようにセクションごとに時系列通りに詳細に書いてください。
```

#### 12.2.5 タスク担当者特定ロジック

```
議事録テキスト
    │
    ▼
┌─────────────────────────────────────────┐
│ タスク抽出AI                            │
│                                         │
│ 「田中さん、来週までに見積もり出して」   │
│       ↓                                 │
│ タスク: 見積もり作成                     │
│ 担当: 田中さん                          │
│ 期限: 来週（→ 1週間後に正規化）          │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│ 名前マッチング                          │
│                                         │
│ 「田中さん」                            │
│   → chatwork_users.display_name検索     │
│   → 組織図のニックネーム/エイリアス検索  │
│   → 一致したらそのaccount_idに割り当て   │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│ 曖昧な場合は確認                         │
│                                         │
│ 「このタスク誰に割り当てる？」           │
│ ・見積もり作成（担当: 田中さん？）       │
│   → 田中太郎 / 田中花子 / スキップ       │
└─────────────────────────────────────────┘
```

#### 12.2.6 脳への統合

議事録をソウルくんの記憶に統合し、今後の立ち回りに活用する。

```
【議事録 → 脳への統合】

┌─────────────────────────────────────────────────────────┐
│                    議事録                               │
│  ■新規事業について（00:00〜）                           │
│  カズさんから「スピード重視で進めよう」という方針が...   │
└───────────────────────────┬─────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│ エピソード記憶 │   │  CEO Learning │   │  知識グラフ   │
│ (Phase 2G)    │   │  (Phase 2D)   │   │  (Phase 2G)   │
│               │   │               │   │               │
│ 「1/27の会議で│   │ カズさんの教え│   │ 新規事業      │
│  新規事業の   │   │ 「スピード重視│   │   ├─ 方針:速度優先│
│  方針が決定」 │   │  で進めよう」 │   │   ├─ 担当:田中,佐藤│
│               │   │  → 全社に適用 │   │   └─ 決定日:1/27│
└───────────────┘   └───────────────┘   └───────────────┘
```

**活用例:**
- 「先月の〇〇会議で何が決まった？」→ 即答
- 「このプロジェクトの経緯教えて」→ 関連議事録を辿って説明
- 「カズさん最近何て言ってた？」→ 会議での発言から回答
- 「田中さんのタスク状況は？」→ 議事録から抽出したタスク含めて回答

#### 12.2.7 必要なAPI連携

| API | 用途 | ステータス |
|-----|------|-----------|
| Zoom API | OAuth認証、Webhook、録画取得 | 📋 未実装 |
| Google Calendar API | 会議情報・参加者取得 | 📋 未実装 |
| Whisper API | 音声文字起こし | ✅ Phase M2で実装済み |
| ChatWork API | メッセージ送信、タスク追加 | ✅ 既存機能 |

#### 12.2.8 運用パラメータ

| 項目 | 値 |
|------|-----|
| Zoomアカウント数 | 5〜6個 |
| 録画頻度（月曜） | 約10本 |
| 録画頻度（他の曜日） | 1日2〜3本 |
| タスク期限デフォルト | 1週間 |

#### 12.2.9 実装待ちアイテム

- [ ] Zoomアカウント情報（メールアドレス）の取得 → 管理部に確認
- [ ] Zoom OAuth設定
- [ ] Zoom Webhook設定
- [ ] Google Calendar API連携
- [ ] マッピングテーブル設計・初期データ投入
- [ ] 組織図へのエイリアスカラム追加

---

## 13. 付録

### 13.1 用語集

| 用語 | 説明 |
|------|------|
| **Multimodal** | テキスト以外（画像、音声等）も扱えること |
| **ディープリサーチ** | 複数ソースから情報を収集・分析する深い調査 |
| **自律エージェント** | 人間の介入なしにタスクを完了できるAI |
| **ティア** | モデルの品質/コストによる分類 |
| **フォールバック** | 障害時に代替手段に切り替えること |

### 13.2 参考資料

- OpenAI API Documentation
- Anthropic Claude API Documentation
- Google Generative AI Documentation
- DALL-E 3 API Documentation
- Runway API Documentation

### 13.3 変更履歴

| 日付 | バージョン | 変更内容 |
|------|-----------|---------|
| 2026-01-27 | v1.0.0 | 初版作成 |
| 2026-01-27 | v1.1.0 | セクション12追加（統合アプリケーション、App2 Zoom連携設計） |

---

**作成者:** Claude Opus 4.5
**レビュー:**
**承認:**

