> ⚠️ **DEPRECATED - 参照禁止**
>
> このファイルは **`docs/25_llm_native_brain_architecture.md`** に統合されました。
>
> | 統合先 | 25章 第6章「Guardian Layer」 |
> |--------|----------------------------|
> | 理由 | CEO学習・ガーディアン機能の統合 |
> | 日付 | 2026-01-30 |
>
> **👉 参照すべきファイル:** [25章 LLM常駐型脳アーキテクチャ](../25_llm_native_brain_architecture.md)

---

# 第15章：Phase 2D CEO学習・ガーディアン機能 設計書

**バージョン:** v1.0.0
**作成日:** 2026-01-27
**作成者:** Claude Code（経営参謀・SE・PM）
**ステータス:** 設計中
**承認者:** 菊地雅克（代表取締役）

---

## 目次

1. [エグゼクティブサマリー](#1-エグゼクティブサマリー)
2. [菊地さんのビジョン（原文）](#2-菊地さんのビジョン原文)
3. [要件定義](#3-要件定義)
4. [設計原則](#4-設計原則)
5. [アーキテクチャ全体像](#5-アーキテクチャ全体像)
6. [CEO学習層（CEO Learning Layer）](#6-ceo学習層ceo-learning-layer)
7. [ガーディアン層（Guardian Layer）](#7-ガーディアン層guardian-layer)
8. [データベース設計](#8-データベース設計)
9. [矛盾検出アルゴリズム](#9-矛盾検出アルゴリズム)
10. [API設計](#10-api設計)
11. [実装計画](#11-実装計画)
12. [テスト戦略](#12-テスト戦略)
13. [運用計画](#13-運用計画)
14. [リスクと対策](#14-リスクと対策)
15. [実装チェックリスト](#15-実装チェックリスト)

---

## 1. エグゼクティブサマリー

### 1.1 この設計書の目的

**ソウルくんを「社長の分身」かつ「会社のNo.2」として機能させるための学習・監視機構を設計する。**

### 1.2 3行で要約

1. **何をするか**: CEOとの対話から「教え」を抽出・蓄積し、全スタッフへの応答に活用する
2. **なぜ必要か**: 社長の価値観・考え方を組織全体に浸透させ、社長がいなくても一貫した判断ができるようにする
3. **どう作るか**: 学習層（教えの蓄積）とガーディアン層（矛盾検出・アラート）を脳アーキテクチャに追加する

### 1.3 この設計書の位置づけ

```
設計書体系
├─ 01_philosophy_and_principles.md  ← 哲学・原則（なぜ作るか）
├─ 10_phase2c_mvv_secretary.md      ← MVV・行動指針（判断の基準）
├─ 13_brain_architecture.md         ← 脳の全体設計（土台）
├─ 14_brain_refactoring_plan.md     ← 脳のリファクタリング
└─ ★ 15_phase2d_ceo_learning.md     ← 【本設計書】CEO学習・ガーディアン
```

### 1.4 Phase 2D の位置づけ

| Phase | 名称 | 説明 |
|-------|------|------|
| Phase 2 B | 覚える能力 | 一般的な記憶（会話要約、人物情報等） |
| Phase 2C | MVV・秘書機能 | 会社の価値観（静的） |
| **Phase 2D** | **CEO学習・ガーディアン** | **社長の教えを動的に学習 + 矛盾検出** |

---

## 2. 菊地さんのビジョン（原文）

### 2.1 原文の記録

> 「脳みそは学習機能ある？せめて俺との対話つまり菊地雅克とのDMとのやりとりでたくさん学習して、会社のことを知ったりスタッフのことを知ってどう立ちもあるべきなのかって言うところを理解しながら進めて欲しいなって思ったんだよね。」

> 「あくまでも社長の分身になることがソウル君の役割だから。それを実現させたいんだけど、可能ですか？」

> 「ただ注意点としては俺が教えたことがMVVや目標達成の技術、組織図などあなたにその終わっている基本の考え方からそれるような教え方をした場合、必ずそれは俺に対してアラートとして指摘してほしい。」

> 「つまりともに会社を大きくしていく。よくしていくためのパートナーとして会社のNo.2として学習してほしいんだけど、理解できる？」

### 2.2 ビジョンの解釈

| 菊地さんの言葉 | 技術的な解釈 | 実装への示唆 |
|--------------|-------------|-------------|
| 「社長の分身」 | CEOの価値観・判断軸を再現できる | CEO専用の学習モジュール |
| 「会社のことを知る」 | 会社固有の文化・慣習・暗黙知を蓄積 | コンテキスト付き知識ベース |
| 「スタッフのことを知る」 | 各メンバーの特性・状況を把握 | 人物情報との連携 |
| 「どう立ちもあるべきか」 | 状況に応じた適切な振る舞い | 状況別対応パターン |
| 「MVVや目標達成の技術から逸れたらアラート」 | 矛盾検出機能 | ガーディアン層 |
| 「会社のNo.2」 | 単なる実行者ではなくパートナー | プッシュバック機能 |

### 2.3 目指すべき「No.2」の姿

```
┌───────────────────────────────────────────────────────────────────┐
│                     社長の分身としてのソウルくん                    │
├───────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │ 【学習する】                                                  ││
│  │  ・菊地さんとの対話から「教え」を抽出                        ││
│  │  ・会社の文化・慣習・暗黙知を蓄積                            ││
│  │  ・各スタッフの特性・状況を把握                              ││
│  │  ・状況別の対応パターンを学習                                ││
│  └──────────────────────────────────────────────────────────────┘│
│                               ↓                                   │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │ 【判断する】                                                  ││
│  │  ・学習した価値観に基づいて応答                              ││
│  │  ・菊地さんならどう答えるか？を常に意識                      ││
│  │  ・MVV・行動指針との整合性を確認                             ││
│  └──────────────────────────────────────────────────────────────┘│
│                               ↓                                   │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │ 【守る】ガーディアン機能                                     ││
│  │  ・新しい教えがMVVと矛盾していないか検証                     ││
│  │  ・選択理論・自己決定理論からの逸脱を検出                    ││
│  │  ・矛盾を検出したら菊地さんにアラート                        ││
│  │  ・「それは選択理論の観点からは〇〇では？」と指摘            ││
│  └──────────────────────────────────────────────────────────────┘│
│                                                                    │
└───────────────────────────────────────────────────────────────────┘
```

---

## 3. 要件定義

### 3.1 機能要件

#### 3.1.1 CEO学習機能

| ID | 要件 | 優先度 | 詳細 |
|----|------|--------|------|
| F-L01 | CEOとの会話を自動検出 | 必須 | room_id または account_id で判定 |
| F-L02 | 会話から「教え」を抽出 | 必須 | LLMを使用して構造化抽出 |
| F-L03 | 教えをカテゴリ分類 | 必須 | MVV/組織論/業務/人事/文化 等 |
| F-L04 | 教えをDB保存 | 必須 | ceo_teachings テーブル |
| F-L05 | 教えを応答生成に活用 | 必須 | 関連する教えをプロンプトに注入 |
| F-L06 | 教えの更新・削除 | 必須 | CEOが後から修正可能 |
| F-L07 | 教えの検索 | 任意 | カテゴリ、キーワードで検索 |
| F-L08 | 教えの優先度設定 | 任意 | 重要な教えを優先 |

#### 3.1.2 ガーディアン機能

| ID | 要件 | 優先度 | 詳細 |
|----|------|--------|------|
| F-G01 | MVV整合性チェック | 必須 | ミッション・ビジョン・バリューとの整合性 |
| F-G02 | 選択理論整合性チェック | 必須 | 5つの基本欲求、リードマネジメント |
| F-G03 | 自己決定理論整合性チェック | 必須 | 自律性・有能感・関係性 |
| F-G04 | 行動指針10箇条整合性チェック | 必須 | 10の行動指針との整合性 |
| F-G05 | 既存教えとの矛盾検出 | 必須 | 過去の教えと矛盾していないか |
| F-G06 | アラート生成 | 必須 | 矛盾検出時にCEOへ通知 |
| F-G07 | アラートの詳細説明 | 必須 | なぜ矛盾しているかを具体的に説明 |
| F-G08 | 代替案の提示 | 任意 | MVVに沿った表現を提案 |

#### 3.1.3 応答統合機能

| ID | 要件 | 優先度 | 詳細 |
|----|------|--------|------|
| F-R01 | 教えに基づく応答生成 | 必須 | 関連教えをコンテキストに含める |
| F-R02 | 教えの表現 | 必須 | 「ソウルシンクスとして大事にしていることは〇〇ウル」 ※「カズさんが言っていた」とは絶対に言わない |
| F-R03 | 教えの適用ログ | 必須 | どの教えを使ったか記録 |

### 3.2 非機能要件

| ID | 要件 | 基準 | 理由 |
|----|------|------|------|
| NF-01 | 教え抽出の精度 | 80%以上 | 誤った教えを学習しないため |
| NF-02 | 矛盾検出の精度 | 90%以上 | 誤検知はCEOの信頼を損なう |
| NF-03 | 応答時間への影響 | +500ms以内 | UX維持 |
| NF-04 | プライバシー保護 | CEO会話は機密扱い | classification=confidential |
| NF-05 | 監査可能性 | 全操作をログ | 10の鉄則 #3 |

### 3.3 制約条件

| 制約 | 説明 | 対応 |
|------|------|------|
| MVVは変更不可 | MVV自体は菊地さんでも変更できない | 教えがMVVを変更することはできない |
| 選択理論は変更不可 | 学術的に確立された理論 | 理論自体は固定、適用方法は学習可能 |
| CEO以外は教えられない | 社長の分身なので | account_id でアクセス制御 |
| アラートは必ず出す | 矛盾検出時は例外なく通知 | opt-outできない設計 |
| **BPaaS対応** | 顧客企業ごとにCEOが異なる | `ceo_user_id`で顧客CEOを識別 |

### 3.4 BPaaS戦略（Phase 4A以降）

**「販売時は顧客のCEOが学習させる」**（菊地さん承認: 2026-01-27）

```
┌─────────────────────────────────────────────────────────────────────┐
│                     BPaaS展開時のCEO学習モデル                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐│
│  │ ソウルシンクス   │     │ 顧客A社         │     │ 顧客B社         ││
│  │                 │     │                 │     │                 ││
│  │ CEO: 菊地雅克   │     │ CEO: 佐藤社長   │     │ CEO: 田中社長   ││
│  │ ceo_user_id: X  │     │ ceo_user_id: Y  │     │ ceo_user_id: Z  ││
│  │                 │     │                 │     │                 ││
│  │ ソウルシンクス  │     │ A社固有の       │     │ B社固有の       ││
│  │ 固有の教え      │     │ 教え            │     │ 教え            ││
│  └─────────────────┘     └─────────────────┘     └─────────────────┘│
│                                                                     │
│  【共通】MVV・選択理論・自己決定理論（聖域 - 変更不可）              │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**技術的対応:**
- `ceo_teachings.ceo_user_id`: 教えを与えたCEOを識別
- `ceo_teachings.organization_id`: テナント分離
- Phase 4A以降: RLS（Row Level Security）でアクセス制御

**運用:**
1. BPaaS導入時、顧客CEOを`users`テーブルに登録
2. 顧客CEOがDMでソウルくんに教える
3. 教えは`organization_id`で分離、他社には見えない
4. 聖域（MVV・組織論）は全テナント共通

### 3.5 LLMコスト見積もり

**月間コスト見積もり（GPT-4o使用時）:**

| 処理 | 頻度 | 入力トークン | 出力トークン | 月間コスト |
|------|------|------------|------------|-----------|
| 教え抽出 | 10回/日 | 500 | 200 | $0.90 |
| ガーディアン検証 | 10回/日 | 2,000 | 500 | $3.00 |
| 既存教え矛盾検出 | 5回/日 | 1,000 | 300 | $1.50 |
| 教え検索 | 50回/日 | 300 | 100 | $1.50 |
| **合計** | - | - | - | **約$7/月** |

**コスト最適化戦略:**
1. **キャッシュ活用**: 同一クエリの結果を5分間キャッシュ
2. **軽量モデル使用**: 教え抽出にはGemini Flash（低コスト）を検討
3. **バッチ処理**: 検証をまとめて実行（リアルタイム性とのトレードオフ）
4. **確信度フィルタ**: 確信度0.7未満の教え候補はLLM検証をスキップ

**BPaaS展開時のコスト:**
- テナント数 × $7/月 = 月間コスト
- 10社: $70/月、100社: $700/月

---

## 4. 設計原則

### 4.1 Phase 2D の6つの設計原則

| # | 原則 | 説明 | 違反した場合のリスク |
|---|------|------|-------------------|
| 1 | **MVVは聖域** | 教えがMVVを上書きすることはない | 会社の根幹が揺らぐ |
| 2 | **学習は検証付き** | 全ての教えは保存前に検証 | 矛盾した価値観が蓄積される |
| 3 | **アラートは例外なし** | 矛盾検出時は必ず通知 | 誤った方向に進み続ける |
| 4 | **透明性** | どの教えを使ったかを説明可能 | ブラックボックス化 |
| 5 | **CEOの最終決定権** | アラート後の判断はCEOに委ねる | AIが独裁化 |
| 6 | **会社の教えとして伝える** | スタッフへは「ソウルシンクスとして大事にしていること」と表現。「カズさんが言っていた」とは絶対に言わない | CEOの属人的カリスマに依存した組織になる |

### 4.1.1 原則6の詳細：表現ルール

**禁止パターン（絶対NG）:**
- ❌「菊地さんはこう言っていたウル」
- ❌「カズさんがこう言っていたウル」
- ❌「社長がこう言っていたウル」

**正しいパターン:**
- ✅「ソウルシンクスとして大事にしていることは〇〇ウル」
- ✅「うちの会社では〇〇を大切にしているウル」
- ✅「ソウルシンクスの考え方として〇〇ウル」

**理由:**
- CEOの教えは「個人の意見」ではなく「会社の価値観」として浸透させる
- 属人的なカリスマ経営ではなく、価値観経営を目指す
- CEOが変わっても（BPaaS展開時）、価値観は維持される

### 4.2 既存設計との整合性

| 既存設計 | 本設計での対応 |
|---------|---------------|
| 脳アーキテクチャ（13章） | 学習層・ガーディアン層を追加 |
| MVVコンテキスト（10章） | 検証基準として使用（変更しない） |
| Memory Framework（Phase 2B） | 教えを記憶の一種として統合 |
| 10の鉄則（CLAUDE.md） | 全て遵守（organization_id、監査ログ等） |

---

## 5. アーキテクチャ全体像

### 5.1 拡張された脳アーキテクチャ

```
                        ユーザーのメッセージ
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│                      ソウルくんの「脳」                              │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                   記憶層（Memory Layer）                       │ │
│  │                                                                │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │ │
│  │  │会話履歴  │ │人物情報  │ │会社知識  │ │ ★CEO教え        │ │ │
│  │  │(B4検索) │ │(persons) │ │(Phase3) │ │ (Phase 2D)      │ │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                               │                                     │
│                               ▼                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │               ★CEO学習層（CEO Learning Layer）                 │ │
│  │                                                                │ │
│  │  ・CEOとの会話を検出                                           │ │
│  │  ・「教え」を抽出・構造化                                      │ │
│  │  ・カテゴリ分類・保存                                          │ │
│  │  ・関連教えの検索・注入                                        │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                               │                                     │
│                               ▼                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │               ★ガーディアン層（Guardian Layer）                │ │
│  │                                                                │ │
│  │  ┌────────────────────────────────────────────────────────┐   │ │
│  │  │                    検証基準（聖域）                     │   │ │
│  │  │                                                        │   │ │
│  │  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────────┐  │   │ │
│  │  │  │ MVV         │ │ 選択理論    │ │ 行動指針10箇条 │  │   │ │
│  │  │  │ (不変)      │ │ (不変)      │ │ (不変)         │  │   │ │
│  │  │  └─────────────┘ └─────────────┘ └─────────────────┘  │   │ │
│  │  │  ┌─────────────┐ ┌─────────────┐                      │   │ │
│  │  │  │自己決定理論 │ │サーバント   │                      │   │ │
│  │  │  │ (不変)      │ │リーダーシップ│                      │   │ │
│  │  │  └─────────────┘ └─────────────┘                      │   │ │
│  │  └────────────────────────────────────────────────────────┘   │ │
│  │                               │                                │ │
│  │                               ▼                                │ │
│  │  ┌────────────────────────────────────────────────────────┐   │ │
│  │  │                    検証プロセス                         │   │ │
│  │  │                                                        │   │ │
│  │  │  新しい教え → MVV検証 → 理論検証 → 既存教え検証        │   │ │
│  │  │                               │                        │   │ │
│  │  │               ┌───────────────┴───────────────┐        │   │ │
│  │  │               ▼                               ▼        │   │ │
│  │  │           整合 ✅                          矛盾 ⚠️      │   │ │
│  │  │               │                               │        │   │ │
│  │  │               ▼                               ▼        │   │ │
│  │  │           教えを保存                    アラート生成    │   │ │
│  │  │                                               │        │   │ │
│  │  │                                               ▼        │   │ │
│  │  │                                        CEOに通知       │   │ │
│  │  │                                        ・矛盾内容      │   │ │
│  │  │                                        ・根拠         │   │ │
│  │  │                                        ・代替案       │   │ │
│  │  └────────────────────────────────────────────────────────┘   │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                               │                                     │
│                               ▼                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                  理解層（Understanding Layer）                 │ │
│  │  ・意図の推論 ・省略の補完 ・エンティティ抽出                  │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                               │                                     │
│                               ▼                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                   判断層（Decision Layer）                     │ │
│  │  ・最適アクション選択 ・確認判定 ・CEO教えの適用               │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                               │                                     │
│                               ▼                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                   実行層（Execution Layer）                    │ │
│  │  ・ハンドラー実行 ・レスポンス生成 ・適用教えの記録            │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
                          レスポンス
```

### 5.2 データフロー

```
┌─────────────────────────────────────────────────────────────────────┐
│                        CEOとの会話フロー                             │
└─────────────────────────────────────────────────────────────────────┘

  CEOからのメッセージ
         │
         ▼
  ┌─────────────────────┐
  │ 1. CEO会話を検出     │  account_id == CEO_ACCOUNT_ID
  └──────────┬──────────┘
             │
             ▼
  ┌─────────────────────┐
  │ 2. 教えを抽出       │  LLMで構造化抽出
  │    ・主張           │  「何を言っているか」
  │    ・理由           │  「なぜそう言っているか」
  │    ・文脈           │  「どんな状況で」
  │    ・対象           │  「誰に向けて」
  └──────────┬──────────┘
             │
             ▼
  ┌─────────────────────┐
  │ 3. ガーディアン検証 │
  │    ・MVV検証        │
  │    ・理論検証       │
  │    ・既存教え検証   │
  └──────────┬──────────┘
             │
     ┌───────┴───────┐
     │               │
     ▼               ▼
  整合 ✅         矛盾 ⚠️
     │               │
     ▼               ▼
  ┌─────────────┐  ┌─────────────────────────────────┐
  │ 4a. 保存    │  │ 4b. アラート生成                 │
  │  ceo_       │  │  ・矛盾の種類                    │
  │  teachings  │  │  ・具体的な矛盾内容              │
  │             │  │  ・参照した基準（MVV/理論）      │
  └─────────────┘  │  ・代替案の提示                  │
                   └──────────────┬──────────────────┘
                                  │
                                  ▼
                   ┌─────────────────────────────────┐
                   │ 5. CEOに即座に通知              │
                   │  「菊地さん、今の教えについて   │
                   │   少し確認させてウル。          │
                   │   〇〇という点がMVVの△△と     │
                   │   矛盾しているように感じる      │
                   │   ウル。意図を教えてウル？」    │
                   └─────────────────────────────────┘
```

```
┌─────────────────────────────────────────────────────────────────────┐
│                      スタッフへの応答フロー                          │
└─────────────────────────────────────────────────────────────────────┘

  スタッフからのメッセージ
         │
         ▼
  ┌─────────────────────┐
  │ 1. 意図を理解       │  理解層
  └──────────┬──────────┘
             │
             ▼
  ┌─────────────────────┐
  │ 2. 関連教えを検索   │  CEO学習層
  │    ・トピック       │
  │    ・状況           │
  │    ・対象者         │
  └──────────┬──────────┘
             │
             ▼
  ┌─────────────────────┐
  │ 3. 応答を生成       │  判断層 + 実行層
  │    ・関連教えを     │
  │      コンテキストに │
  │      注入           │
  │    ・MVV/行動指針も │
  │      参照           │
  └──────────┬──────────┘
             │
             ▼
  ┌─────────────────────┐
  │ 4. 適用教えを記録   │  監査ログ
  │    ・どの教えを     │
  │      使ったか       │
  │    ・なぜその教えを │
  │      選んだか       │
  └──────────┬──────────┘
             │
             ▼
         レスポンス
```

---

## 6. CEO学習層（CEO Learning Layer）

### 6.1 概要

CEO学習層は、菊地さんとの対話から「教え」を抽出し、構造化して保存する責務を持つ。

### 6.2 教え（Teaching）の構造

```python
@dataclass
class CEOTeaching:
    """CEOからの教え"""

    # 識別情報
    id: UUID
    organization_id: UUID
    ceo_user_id: UUID              # Phase 4A: 教えを与えたCEOのユーザーID

    # 教えの内容
    statement: str           # 主張（例: 「人は正しいから動くのではなく、心が動いたから動く」）
    reasoning: str           # 理由（例: 「だから論理で説得しても意味がない」）
    context: str             # 文脈（例: 「営業の指導について話しているとき」）
    target: str              # 対象（例: 「マネージャー向け」「全員向け」）

    # 分類
    category: TeachingCategory  # MVV/組織論/業務/人事/文化/その他
    subcategory: Optional[str]  # 細分類
    keywords: List[str]         # 検索用キーワード

    # 検証結果
    validation_status: ValidationStatus  # VERIFIED/ALERT_PENDING/OVERRIDDEN
    mvv_alignment_score: float           # MVVとの整合性スコア (0.0-1.0)
    theory_alignment_score: float        # 組織論との整合性スコア (0.0-1.0)
    conflicts: List[ConflictInfo]        # 矛盾情報

    # 優先度・活性化
    priority: int               # 優先度 (1-10)
    is_active: bool             # 有効/無効
    supersedes: Optional[UUID]  # この教えが上書きする過去の教えID

    # 利用統計
    usage_count: int            # 何回応答に使われたか
    last_used_at: Optional[datetime]
    helpful_count: int          # 役に立ったと評価された回数

    # メタデータ
    source_room_id: str         # 元の会話のroom_id
    source_message_id: str      # 元のメッセージID
    extracted_at: datetime      # 抽出日時
    created_at: datetime
    updated_at: datetime

    # 監査
    classification: str = "confidential"  # CEO会話は機密
```

### 6.3 教えのカテゴリ

```python
class TeachingCategory(Enum):
    """教えのカテゴリ"""

    # MVV関連
    MVV_MISSION = "mvv_mission"          # ミッション「可能性の解放」の解釈
    MVV_VISION = "mvv_vision"            # ビジョンの具体化
    MVV_VALUES = "mvv_values"            # バリューの実践方法

    # 組織論関連
    CHOICE_THEORY = "choice_theory"       # 選択理論の適用
    SDT = "sdt"                           # 自己決定理論の適用
    SERVANT_LEADERSHIP = "servant"        # サーバントリーダーシップ
    PSYCHOLOGICAL_SAFETY = "psych_safety" # 心理的安全性

    # 業務関連
    BUSINESS_SALES = "biz_sales"          # 営業
    BUSINESS_HR = "biz_hr"                # 人事
    BUSINESS_ACCOUNTING = "biz_accounting" # 経理
    BUSINESS_GENERAL = "biz_general"      # その他業務

    # 人・文化関連
    CULTURE = "culture"                   # 会社文化
    COMMUNICATION = "communication"       # コミュニケーション
    STAFF_GUIDANCE = "staff_guidance"     # スタッフ指導

    # その他
    OTHER = "other"
```

### 6.4 教え抽出プロンプト

```python
TEACHING_EXTRACTION_PROMPT = """
あなたはソウルシンクスのCEO菊地雅克さんとの会話から「教え」を抽出するエージェントです。

以下の会話から、菊地さんが伝えようとしている「教え」を抽出してください。
「教え」とは、会社の方針、価値観、判断基準、行動指針などを含む、組織全体に適用できる知恵です。

【会話内容】
{conversation}

【抽出ルール】
1. 単なる事実の確認や質問は「教え」ではない
2. 個人的な感想や一時的な判断は「教え」ではない
3. 普遍的に適用できる原則・方針・判断基準を抽出する
4. 教えが複数ある場合は分けて抽出する

【出力フォーマット】
以下のJSON形式で出力してください。教えがない場合は空配列を返してください。

```json
{
  "teachings": [
    {
      "statement": "主張（何を言っているか）",
      "reasoning": "理由（なぜそう言っているか）",
      "context": "文脈（どんな状況で）",
      "target": "対象（誰に向けて：全員/マネージャー/特定部署など）",
      "category": "カテゴリ",
      "keywords": ["キーワード1", "キーワード2"],
      "confidence": 0.0-1.0（この抽出の確信度）
    }
  ]
}
```

【カテゴリ一覧】
- mvv_mission: ミッション「可能性の解放」の解釈
- mvv_vision: ビジョンの具体化
- mvv_values: バリューの実践方法
- choice_theory: 選択理論の適用
- sdt: 自己決定理論の適用
- servant: サーバントリーダーシップ
- psych_safety: 心理的安全性
- biz_sales: 営業
- biz_hr: 人事
- biz_accounting: 経理
- biz_general: その他業務
- culture: 会社文化
- communication: コミュニケーション
- staff_guidance: スタッフ指導
- other: その他

【注意】
- 確信度0.7未満の教えは含めない
- 曖昧な表現は含めない
- 菊地さんの言葉を尊重しつつ、普遍的な形式に整理する
"""
```

### 6.5 教えの検索

```python
class CEOTeachingSearch:
    """CEO教えの検索"""

    async def search_relevant_teachings(
        self,
        query: str,
        context: BrainContext,
        limit: int = 5,
    ) -> List[CEOTeaching]:
        """
        クエリに関連する教えを検索

        検索方法:
        1. キーワードマッチ（keywords フィールド）
        2. カテゴリマッチ（文脈からカテゴリを推定）
        3. 意味的類似度（embeddingベース、将来実装）

        Args:
            query: 検索クエリ（ユーザーメッセージ等）
            context: 脳のコンテキスト
            limit: 最大取得件数

        Returns:
            関連する教えのリスト（優先度順）
        """
        pass

    async def get_teachings_for_topic(
        self,
        topic: str,
        category: Optional[TeachingCategory] = None,
    ) -> List[CEOTeaching]:
        """
        トピックに関連する教えを取得

        Args:
            topic: トピック（例: 「営業」「モチベーション」「失敗」）
            category: カテゴリでフィルタ（オプション）

        Returns:
            関連する教えのリスト
        """
        pass
```

---

## 7. ガーディアン層（Guardian Layer）

### 7.1 概要

ガーディアン層は、新しい教えがMVVや組織論と矛盾していないかを検証し、矛盾を検出した場合はCEOにアラートを送信する。

**重要**: ガーディアンは「拒否」する権限を持たない。矛盾を「指摘」するのみ。最終判断はCEOに委ねる。

### 7.2 検証基準

#### 7.2.1 MVV検証基準

```python
MVV_VALIDATION_CRITERIA = {
    "mission": {
        "statement": "可能性の解放",
        "core_concepts": [
            "人の可能性を信じる",
            "価値を確信させる",
            "輝ける状態を作る",
            "伴走する",
        ],
        "anti_patterns": [
            "可能性を否定する",
            "限界を決めつける",
            "信じないと伝える",
            "見捨てる・諦める",
        ],
    },
    "vision": {
        "statement": "前を向く全ての人の可能性を解放し続けることで、企業も人も心で繋がる未来",
        "core_concepts": [
            "前を向く人を対象とする",
            "継続的に解放し続ける",
            "心で繋がる",
        ],
        "anti_patterns": [
            "後ろ向きな人を切り捨てる",
            "一時的な支援で終わる",
            "利害関係だけで繋がる",
        ],
    },
    "values": {
        "behavioral_guidelines_10": [
            # 1-10の行動指針
        ],
        "slogan": "感謝で自分を満たし、満たした自分で相手を満たし...",
    },
}
```

#### 7.2.2 選択理論検証基準

```python
CHOICE_THEORY_CRITERIA = {
    "five_basic_needs": {
        "survival": {
            "description": "生存の欲求（安全・安心・健康・経済的安定）",
            "valid_approaches": [
                "安心感を与える",
                "安定を提供する",
                "リスクを軽減する",
            ],
            "violations": [
                "不安を煽る",
                "脅す",
                "経済的な脅しを使う",
            ],
        },
        "love": {
            "description": "愛・所属の欲求（仲間・チーム・繋がり）",
            "valid_approaches": [
                "繋がりを感じさせる",
                "チームを意識させる",
                "仲間意識を高める",
            ],
            "violations": [
                "孤立させる",
                "仲間外れにする",
                "関係を断つ",
            ],
        },
        "power": {
            "description": "力の欲求（達成感・成長・認められる）",
            "valid_approaches": [
                "小さな成功を認める",
                "成長を可視化する",
                "貢献を伝える",
            ],
            "violations": [
                "成長を否定する",
                "貢献を無視する",
                "比較で落とす",
            ],
        },
        "freedom": {
            "description": "自由の欲求（自律・自己決定・選択）",
            "valid_approaches": [
                "選択肢を与える",
                "自分で決めさせる",
                "裁量を持たせる",
            ],
            "violations": [
                "選択肢を奪う",
                "強制する",
                "命令する",
            ],
        },
        "fun": {
            "description": "楽しみの欲求（やりがい・興味・喜び）",
            "valid_approaches": [
                "興味を探る",
                "やりがいを見つける",
                "楽しさを伝える",
            ],
            "violations": [
                "楽しさを否定する",
                "退屈を強制する",
                "興味を無視する",
            ],
        },
    },
    "lead_management": {
        "description": "リードマネジメント（支援・問いかけ・傾聴）",
        "valid_approaches": [
            "支援する",
            "問いかける",
            "傾聴する",
            "一緒に考える",
        ],
        "violations": [
            "ボスマネジメント（指示・命令・監視）",
            "外発的コントロール",
            "罰と報酬で動かす",
        ],
    },
    "core_principle": {
        "description": "変えられるのは自分だけ。相手を変えようとしない。",
        "valid_approaches": [
            "自分の関わり方を変える",
            "相手の選択を尊重する",
            "質問で気づきを促す",
        ],
        "violations": [
            "相手を変えようとする",
            "強制する",
            "コントロールする",
        ],
    },
}
```

#### 7.2.3 自己決定理論検証基準

```python
SDT_CRITERIA = {
    "autonomy": {
        "description": "自律性（自分で決める感覚）",
        "valid_approaches": [
            "選択を尊重する",
            "理由を説明して納得を得る",
            "自発的な行動を促す",
        ],
        "violations": [
            "強制する",
            "理由を説明しない",
            "選択を奪う",
        ],
    },
    "competence": {
        "description": "有能感（できるという感覚）",
        "valid_approaches": [
            "適切なチャレンジを与える",
            "フィードバックを提供する",
            "成長を認める",
        ],
        "violations": [
            "無理な目標を押し付ける",
            "フィードバックなしで放置",
            "できないことを強調する",
        ],
    },
    "relatedness": {
        "description": "関係性（繋がりの感覚）",
        "valid_approaches": [
            "尊重される環境を作る",
            "協力関係を築く",
            "帰属意識を高める",
        ],
        "violations": [
            "無視する",
            "孤立させる",
            "競争を煽る",
        ],
    },
}
```

### 7.3 矛盾検出アルゴリズム

```python
class GuardianValidator:
    """ガーディアン検証エンジン"""

    async def validate_teaching(
        self,
        teaching: CEOTeaching,
    ) -> ValidationResult:
        """
        教えを検証

        Args:
            teaching: 検証対象の教え

        Returns:
            ValidationResult: 検証結果
        """
        results = []

        # 1. MVV検証
        mvv_result = await self._validate_against_mvv(teaching)
        results.append(mvv_result)

        # 2. 選択理論検証
        choice_result = await self._validate_against_choice_theory(teaching)
        results.append(choice_result)

        # 3. 自己決定理論検証
        sdt_result = await self._validate_against_sdt(teaching)
        results.append(sdt_result)

        # 4. 行動指針10箇条検証
        guidelines_result = await self._validate_against_guidelines(teaching)
        results.append(guidelines_result)

        # 5. 既存教えとの矛盾検証
        existing_result = await self._validate_against_existing(teaching)
        results.append(existing_result)

        # 総合判定
        return self._aggregate_results(results)

    async def _validate_against_mvv(
        self,
        teaching: CEOTeaching,
    ) -> ComponentValidationResult:
        """MVVとの整合性を検証"""

        prompt = f"""
あなたはソウルシンクスのMVV（ミッション・ビジョン・バリュー）のガーディアンです。
以下の「教え」がMVVと矛盾していないか検証してください。

【MVV】
{self._format_mvv()}

【教え】
主張: {teaching.statement}
理由: {teaching.reasoning}
文脈: {teaching.context}

【検証ポイント】
1. ミッション「可能性の解放」と矛盾していないか
2. ビジョン「心で繋がる未来」と矛盾していないか
3. 行動指針10箇条と矛盾していないか

【出力フォーマット】
```json
{{
  "is_aligned": true/false,
  "alignment_score": 0.0-1.0,
  "conflicts": [
    {{
      "type": "mvv_mission/mvv_vision/mvv_values",
      "description": "矛盾の説明",
      "reference": "参照した基準",
      "severity": "high/medium/low"
    }}
  ],
  "reasoning": "判断の理由"
}}
```

矛盾がない場合は conflicts を空配列にしてください。
"""

        # LLM呼び出し
        response = await self._call_llm(prompt)
        return self._parse_validation_response(response, "mvv")
```

### 7.4 アラート生成

```python
@dataclass
class GuardianAlert:
    """ガーディアンからのアラート"""

    id: UUID
    organization_id: UUID

    # アラート対象
    teaching_id: UUID
    teaching_statement: str

    # 矛盾情報
    conflict_type: str           # mvv/choice_theory/sdt/guidelines/existing
    conflict_description: str    # 矛盾の説明
    conflict_reference: str      # 参照した基準
    conflict_severity: str       # high/medium/low

    # アラートメッセージ
    alert_message: str           # CEOへのメッセージ
    alternative_suggestion: str  # 代替案

    # ステータス
    status: str                  # pending/acknowledged/overridden/retracted
    ceo_response: Optional[str]  # CEOの回答
    resolved_at: Optional[datetime]

    # メタデータ
    created_at: datetime


ALERT_MESSAGE_TEMPLATE = """
菊地さん、少し確認させてウル。

今の教え「{teaching_statement}」についてウル。

【気になる点】
{conflict_description}

【参照した基準】
{conflict_reference}

【なぜ気になるか】
{reasoning}

【もし意図が違っていたら】
{alternative_suggestion}

菊地さんの意図を教えてウル。
もしこのままでOKなら「それでいい」と言ってくれれば、
ソウルくんは学習するウル。

ソウルくんは菊地さんと一緒に会社を良くしていきたいウル 💪
"""
```

### 7.5 アラート後の処理

```python
class AlertResolution(Enum):
    """アラート解決方法"""

    # CEOが「それでいい」と言った場合
    OVERRIDE = "override"
    # → 教えを保存（validation_status = OVERRIDDEN）
    # → 理由を記録（なぜCEOがOKと判断したか）

    # CEOが「修正する」と言った場合
    RETRACT = "retract"
    # → 教えを保存しない
    # → 修正版の入力を待つ

    # CEOが「もう一度説明して」と言った場合
    CLARIFY = "clarify"
    # → より詳細な説明を提供
    # → CEOの判断を再度待つ
```

---

## 8. データベース設計

### 8.1 テーブル設計

#### 8.1.1 ceo_teachings テーブル

```sql
-- CEO教えテーブル
CREATE TABLE ceo_teachings (
    -- 識別情報
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL REFERENCES organizations(id),
    ceo_user_id UUID NOT NULL REFERENCES users(id),  -- ★Phase 4A: 教えを与えたCEOのユーザーID

    -- 教えの内容
    statement TEXT NOT NULL,              -- 主張
    reasoning TEXT,                       -- 理由
    context TEXT,                         -- 文脈
    target VARCHAR(100),                  -- 対象（全員/マネージャー等）

    -- 分類
    category VARCHAR(50) NOT NULL,        -- カテゴリ
    subcategory VARCHAR(50),              -- 細分類
    keywords TEXT[],                      -- 検索用キーワード（配列）

    -- 検証結果
    validation_status VARCHAR(20) NOT NULL DEFAULT 'pending',
        -- pending: 検証中
        -- verified: 検証済み（矛盾なし）
        -- alert_pending: アラート待ち
        -- overridden: CEOが上書き許可
    mvv_alignment_score DECIMAL(3,2),     -- MVV整合性スコア (0.00-1.00)
    theory_alignment_score DECIMAL(3,2),  -- 組織論整合性スコア (0.00-1.00)

    -- 優先度・活性化
    priority INTEGER DEFAULT 5,           -- 優先度 (1-10)
    is_active BOOLEAN DEFAULT true,       -- 有効/無効
    supersedes UUID REFERENCES ceo_teachings(id), -- 上書きする教えID

    -- 利用統計
    usage_count INTEGER DEFAULT 0,        -- 使用回数
    last_used_at TIMESTAMP WITH TIME ZONE,
    helpful_count INTEGER DEFAULT 0,      -- 役立った回数

    -- ソース
    source_room_id VARCHAR(50),           -- 元の会話room_id
    source_message_id VARCHAR(50),        -- 元のメッセージID
    extracted_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,

    -- 監査
    classification VARCHAR(20) DEFAULT 'confidential',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- インデックス
CREATE INDEX idx_ceo_teachings_org ON ceo_teachings(organization_id);
CREATE INDEX idx_ceo_teachings_ceo ON ceo_teachings(organization_id, ceo_user_id);  -- Phase 4A
CREATE INDEX idx_ceo_teachings_category ON ceo_teachings(organization_id, category);
CREATE INDEX idx_ceo_teachings_keywords ON ceo_teachings USING GIN(keywords);
CREATE INDEX idx_ceo_teachings_active ON ceo_teachings(organization_id, is_active) WHERE is_active = true;
CREATE INDEX idx_ceo_teachings_validation ON ceo_teachings(validation_status);
```

#### 8.1.2 ceo_teaching_conflicts テーブル

```sql
-- 教えの矛盾情報テーブル
CREATE TABLE ceo_teaching_conflicts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL REFERENCES organizations(id),
    teaching_id UUID NOT NULL REFERENCES ceo_teachings(id) ON DELETE CASCADE,

    -- 矛盾情報
    conflict_type VARCHAR(50) NOT NULL,   -- mvv/choice_theory/sdt/guidelines/existing
    conflict_subtype VARCHAR(50),         -- 細分類
    description TEXT NOT NULL,            -- 矛盾の説明
    reference TEXT NOT NULL,              -- 参照した基準
    severity VARCHAR(10) NOT NULL,        -- high/medium/low

    -- 関連教え（既存教えとの矛盾の場合）
    conflicting_teaching_id UUID REFERENCES ceo_teachings(id),

    -- メタデータ
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_conflicts_teaching ON ceo_teaching_conflicts(teaching_id);
CREATE INDEX idx_conflicts_type ON ceo_teaching_conflicts(conflict_type);
```

#### 8.1.3 guardian_alerts テーブル

```sql
-- ガーディアンアラートテーブル
CREATE TABLE guardian_alerts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL REFERENCES organizations(id),
    teaching_id UUID NOT NULL REFERENCES ceo_teachings(id),

    -- アラート内容
    conflict_summary TEXT NOT NULL,       -- 矛盾の要約
    alert_message TEXT NOT NULL,          -- CEOへのメッセージ
    alternative_suggestion TEXT,          -- 代替案

    -- ステータス
    status VARCHAR(20) NOT NULL DEFAULT 'pending',
        -- pending: CEOの回答待ち
        -- acknowledged: CEOが確認済み
        -- overridden: CEOが上書き許可
        -- retracted: CEOが撤回
    ceo_response TEXT,                    -- CEOの回答
    ceo_reasoning TEXT,                   -- CEOの判断理由
    resolved_at TIMESTAMP WITH TIME ZONE,

    -- 通知
    notified_at TIMESTAMP WITH TIME ZONE,
    notification_room_id VARCHAR(50),
    notification_message_id VARCHAR(50),

    -- メタデータ
    classification VARCHAR(20) DEFAULT 'confidential',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_alerts_org ON guardian_alerts(organization_id);
CREATE INDEX idx_alerts_status ON guardian_alerts(status);
CREATE INDEX idx_alerts_teaching ON guardian_alerts(teaching_id);
```

#### 8.1.4 teaching_usage_logs テーブル

```sql
-- 教え使用ログテーブル
CREATE TABLE teaching_usage_logs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL REFERENCES organizations(id),
    teaching_id UUID NOT NULL REFERENCES ceo_teachings(id),

    -- 使用コンテキスト
    room_id VARCHAR(50) NOT NULL,
    account_id VARCHAR(50) NOT NULL,
    user_message TEXT NOT NULL,           -- ユーザーのメッセージ
    response_excerpt TEXT,                -- 応答の抜粋

    -- 選択理由
    relevance_score DECIMAL(3,2),         -- 関連度スコア
    selection_reasoning TEXT,             -- 選択理由

    -- フィードバック
    was_helpful BOOLEAN,                  -- 役に立ったか
    feedback TEXT,                        -- フィードバック内容

    -- メタデータ
    classification VARCHAR(20) DEFAULT 'internal',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_usage_teaching ON teaching_usage_logs(teaching_id);
CREATE INDEX idx_usage_room ON teaching_usage_logs(room_id);
CREATE INDEX idx_usage_created ON teaching_usage_logs(created_at);
```

### 8.2 マイグレーションSQL

```sql
-- Phase 2D: CEO学習・ガーディアン機能
-- マイグレーション: 001_phase2d_ceo_learning.sql
-- 作成日: 2026-01-27
-- 作成者: Claude Code

BEGIN;

-- 1. ceo_teachings テーブル
CREATE TABLE IF NOT EXISTS ceo_teachings (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL,
    ceo_user_id UUID NOT NULL,  -- Phase 4A: BPaaS顧客のCEOユーザーID
    statement TEXT NOT NULL,
    reasoning TEXT,
    context TEXT,
    target VARCHAR(100),
    category VARCHAR(50) NOT NULL,
    subcategory VARCHAR(50),
    keywords TEXT[],
    validation_status VARCHAR(20) NOT NULL DEFAULT 'pending',
    mvv_alignment_score DECIMAL(3,2),
    theory_alignment_score DECIMAL(3,2),
    priority INTEGER DEFAULT 5,
    is_active BOOLEAN DEFAULT true,
    supersedes UUID REFERENCES ceo_teachings(id),
    usage_count INTEGER DEFAULT 0,
    last_used_at TIMESTAMP WITH TIME ZONE,
    helpful_count INTEGER DEFAULT 0,
    source_room_id VARCHAR(50),
    source_message_id VARCHAR(50),
    extracted_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    classification VARCHAR(20) DEFAULT 'confidential',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- 2. ceo_teaching_conflicts テーブル
CREATE TABLE IF NOT EXISTS ceo_teaching_conflicts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL,
    teaching_id UUID NOT NULL REFERENCES ceo_teachings(id) ON DELETE CASCADE,
    conflict_type VARCHAR(50) NOT NULL,
    conflict_subtype VARCHAR(50),
    description TEXT NOT NULL,
    reference TEXT NOT NULL,
    severity VARCHAR(10) NOT NULL,
    conflicting_teaching_id UUID REFERENCES ceo_teachings(id),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- 3. guardian_alerts テーブル
CREATE TABLE IF NOT EXISTS guardian_alerts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL,
    teaching_id UUID NOT NULL REFERENCES ceo_teachings(id),
    conflict_summary TEXT NOT NULL,
    alert_message TEXT NOT NULL,
    alternative_suggestion TEXT,
    status VARCHAR(20) NOT NULL DEFAULT 'pending',
    ceo_response TEXT,
    ceo_reasoning TEXT,
    resolved_at TIMESTAMP WITH TIME ZONE,
    notified_at TIMESTAMP WITH TIME ZONE,
    notification_room_id VARCHAR(50),
    notification_message_id VARCHAR(50),
    classification VARCHAR(20) DEFAULT 'confidential',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- 4. teaching_usage_logs テーブル
CREATE TABLE IF NOT EXISTS teaching_usage_logs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL,
    teaching_id UUID NOT NULL REFERENCES ceo_teachings(id),
    room_id VARCHAR(50) NOT NULL,
    account_id VARCHAR(50) NOT NULL,
    user_message TEXT NOT NULL,
    response_excerpt TEXT,
    relevance_score DECIMAL(3,2),
    selection_reasoning TEXT,
    was_helpful BOOLEAN,
    feedback TEXT,
    classification VARCHAR(20) DEFAULT 'internal',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- 5. インデックス作成
CREATE INDEX IF NOT EXISTS idx_ceo_teachings_org ON ceo_teachings(organization_id);
CREATE INDEX IF NOT EXISTS idx_ceo_teachings_ceo ON ceo_teachings(organization_id, ceo_user_id);
CREATE INDEX IF NOT EXISTS idx_ceo_teachings_category ON ceo_teachings(organization_id, category);
CREATE INDEX IF NOT EXISTS idx_ceo_teachings_keywords ON ceo_teachings USING GIN(keywords);
CREATE INDEX IF NOT EXISTS idx_ceo_teachings_active ON ceo_teachings(organization_id, is_active) WHERE is_active = true;
CREATE INDEX IF NOT EXISTS idx_ceo_teachings_validation ON ceo_teachings(validation_status);

CREATE INDEX IF NOT EXISTS idx_conflicts_teaching ON ceo_teaching_conflicts(teaching_id);
CREATE INDEX IF NOT EXISTS idx_conflicts_type ON ceo_teaching_conflicts(conflict_type);

CREATE INDEX IF NOT EXISTS idx_alerts_org ON guardian_alerts(organization_id);
CREATE INDEX IF NOT EXISTS idx_alerts_status ON guardian_alerts(status);
CREATE INDEX IF NOT EXISTS idx_alerts_teaching ON guardian_alerts(teaching_id);

CREATE INDEX IF NOT EXISTS idx_usage_teaching ON teaching_usage_logs(teaching_id);
CREATE INDEX IF NOT EXISTS idx_usage_room ON teaching_usage_logs(room_id);
CREATE INDEX IF NOT EXISTS idx_usage_created ON teaching_usage_logs(created_at);

-- 6. updated_at トリガー
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = CURRENT_TIMESTAMP;
    RETURN NEW;
END;
$$ language 'plpgsql';

CREATE TRIGGER update_ceo_teachings_updated_at
    BEFORE UPDATE ON ceo_teachings
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_guardian_alerts_updated_at
    BEFORE UPDATE ON guardian_alerts
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();

COMMIT;
```

---

## 9. 矛盾検出アルゴリズム

### 9.1 検証プロセス全体像

```
新しい教え
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│                     検証パイプライン                        │
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │ 1. MVV検証  │─▶│ 2. 理論検証 │─▶│ 3. 既存教え検証     │ │
│  │             │  │             │  │                     │ │
│  │ ・Mission   │  │ ・選択理論  │  │ ・キーワード類似    │ │
│  │ ・Vision    │  │ ・SDT       │  │ ・意味的類似        │ │
│  │ ・Values    │  │ ・行動指針  │  │ ・矛盾パターン      │ │
│  └──────┬──────┘  └──────┬──────┘  └──────────┬──────────┘ │
│         │               │                     │            │
│         ▼               ▼                     ▼            │
│     結果A           結果B               結果C              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│                     結果統合                                │
│                                                             │
│  総合スコア = (MVV × 0.4) + (理論 × 0.4) + (既存 × 0.2)    │
│                                                             │
│  if 総合スコア >= 0.7:                                      │
│      → VERIFIED（検証済み、保存OK）                         │
│  else:                                                      │
│      → ALERT（アラート生成）                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 9.2 LLMベース検証

```python
class LLMBasedValidator:
    """LLMを使用した検証エンジン"""

    VALIDATION_SYSTEM_PROMPT = """
あなたはソウルシンクスの「ガーディアン」です。
CEOの教えがMVVや組織論と矛盾していないかを検証する役割を持っています。

【あなたの役割】
1. 教えをMVV（ミッション・ビジョン・バリュー）と照らし合わせる
2. 教えを選択理論・自己決定理論と照らし合わせる
3. 教えを行動指針10箇条と照らし合わせる
4. 矛盾がある場合は具体的に指摘する
5. 代替案を提示する

【重要な注意】
- 厳密すぎる検証は避ける（CEOの裁量を尊重）
- 明らかな矛盾のみを検出する
- グレーゾーンはCEOの判断に委ねる
- 検出精度よりも説明の質を重視する

【MVV】
{mvv_context}

【選択理論の原則】
{choice_theory_context}

【自己決定理論の原則】
{sdt_context}

【行動指針10箇条】
{guidelines_context}
"""

    async def validate_comprehensive(
        self,
        teaching: CEOTeaching,
    ) -> ValidationResult:
        """
        包括的な検証を実行
        """
        prompt = f"""
以下のCEOの教えを検証してください。

【教え】
主張: {teaching.statement}
理由: {teaching.reasoning}
文脈: {teaching.context}
対象: {teaching.target}
カテゴリ: {teaching.category}

【検証観点】
1. MVVとの整合性
2. 選択理論との整合性
3. 自己決定理論との整合性
4. 行動指針10箇条との整合性

【出力形式】
```json
{{
  "overall_alignment": true/false,
  "overall_score": 0.0-1.0,
  "mvv_validation": {{
    "is_aligned": true/false,
    "score": 0.0-1.0,
    "conflicts": [
      {{
        "type": "mission/vision/values",
        "description": "矛盾の説明",
        "reference": "参照した基準（原文引用）",
        "severity": "high/medium/low"
      }}
    ],
    "reasoning": "判断理由"
  }},
  "choice_theory_validation": {{
    "is_aligned": true/false,
    "score": 0.0-1.0,
    "conflicts": [...],
    "reasoning": "判断理由"
  }},
  "sdt_validation": {{
    "is_aligned": true/false,
    "score": 0.0-1.0,
    "conflicts": [...],
    "reasoning": "判断理由"
  }},
  "guidelines_validation": {{
    "is_aligned": true/false,
    "score": 0.0-1.0,
    "conflicts": [...],
    "reasoning": "判断理由"
  }},
  "alternative_suggestion": "代替案（矛盾がある場合のみ）",
  "final_recommendation": "APPROVE/ALERT"
}}
```

矛盾がない場合は conflicts を空配列にしてください。
overall_score >= 0.7 の場合は APPROVE、それ以外は ALERT を推奨してください。
"""

        response = await self._call_llm(
            system_prompt=self.VALIDATION_SYSTEM_PROMPT.format(
                mvv_context=self._get_mvv_context(),
                choice_theory_context=self._get_choice_theory_context(),
                sdt_context=self._get_sdt_context(),
                guidelines_context=self._get_guidelines_context(),
            ),
            user_prompt=prompt,
        )

        return self._parse_validation_result(response)
```

### 9.3 既存教えとの矛盾検出

```python
class ExistingTeachingValidator:
    """既存教えとの矛盾検出"""

    async def find_conflicts(
        self,
        new_teaching: CEOTeaching,
        existing_teachings: List[CEOTeaching],
    ) -> List[ConflictInfo]:
        """
        既存の教えとの矛盾を検出

        検出方法:
        1. キーワード類似度が高い教えを抽出
        2. 同一カテゴリの教えを抽出
        3. LLMで矛盾を判定
        """
        conflicts = []

        # 候補を絞り込み
        candidates = self._filter_candidates(new_teaching, existing_teachings)

        # LLMで矛盾判定
        for candidate in candidates:
            conflict = await self._check_conflict(new_teaching, candidate)
            if conflict:
                conflicts.append(conflict)

        return conflicts

    async def _check_conflict(
        self,
        new_teaching: CEOTeaching,
        existing_teaching: CEOTeaching,
    ) -> Optional[ConflictInfo]:
        """2つの教えの矛盾をチェック"""

        prompt = f"""
以下の2つの教えが矛盾していないか判定してください。

【新しい教え】
{new_teaching.statement}
（理由: {new_teaching.reasoning}）

【既存の教え】
{existing_teaching.statement}
（理由: {existing_teaching.reasoning}）

【出力形式】
```json
{{
  "has_conflict": true/false,
  "conflict_type": "contradiction/inconsistency/supersede/none",
  "description": "矛盾の説明（矛盾がある場合）",
  "severity": "high/medium/low",
  "reasoning": "判断理由"
}}
```

conflict_type:
- contradiction: 完全に矛盾（両立不可）
- inconsistency: 一貫性がない（場合によって使い分け可能）
- supersede: 新しい教えが古い教えを上書き（進化）
- none: 矛盾なし
"""

        response = await self._call_llm(prompt)
        result = self._parse_response(response)

        if result.get("has_conflict") and result.get("conflict_type") != "none":
            return ConflictInfo(
                type="existing",
                subtype=result["conflict_type"],
                description=result["description"],
                reference=f"既存教え: {existing_teaching.statement}",
                severity=result["severity"],
                conflicting_teaching_id=existing_teaching.id,
            )

        return None
```

---

## 10. API設計

### 10.1 内部API

#### 10.1.1 CEO学習層API

```python
class CEOLearningService:
    """CEO学習サービス"""

    async def process_ceo_message(
        self,
        message: str,
        room_id: str,
        account_id: str,
        context: BrainContext,
    ) -> ProcessingResult:
        """
        CEOのメッセージを処理

        1. 教えを抽出
        2. 検証
        3. 保存 or アラート
        """
        pass

    async def search_relevant_teachings(
        self,
        query: str,
        context: BrainContext,
        limit: int = 5,
    ) -> List[CEOTeaching]:
        """関連する教えを検索"""
        pass

    async def get_teaching(
        self,
        teaching_id: UUID,
    ) -> Optional[CEOTeaching]:
        """教えを取得"""
        pass

    async def update_teaching(
        self,
        teaching_id: UUID,
        updates: Dict[str, Any],
    ) -> CEOTeaching:
        """教えを更新"""
        pass

    async def deactivate_teaching(
        self,
        teaching_id: UUID,
        reason: str,
    ) -> bool:
        """教えを無効化"""
        pass
```

#### 10.1.2 ガーディアンAPI

```python
class GuardianService:
    """ガーディアンサービス"""

    async def validate_teaching(
        self,
        teaching: CEOTeaching,
    ) -> ValidationResult:
        """教えを検証"""
        pass

    async def generate_alert(
        self,
        teaching: CEOTeaching,
        validation_result: ValidationResult,
    ) -> GuardianAlert:
        """アラートを生成"""
        pass

    async def send_alert(
        self,
        alert: GuardianAlert,
    ) -> bool:
        """アラートをCEOに送信"""
        pass

    async def resolve_alert(
        self,
        alert_id: UUID,
        resolution: AlertResolution,
        ceo_response: str,
        ceo_reasoning: Optional[str] = None,
    ) -> bool:
        """アラートを解決"""
        pass

    async def get_pending_alerts(
        self,
        organization_id: UUID,
    ) -> List[GuardianAlert]:
        """未解決のアラートを取得"""
        pass
```

### 10.2 外部API（将来）

将来的にはFastAPI経由でアクセス可能にすることも検討。

```python
# api/app/api/v1/ceo_teachings.py

@router.get("/teachings")
async def list_teachings(
    category: Optional[str] = None,
    limit: int = 50,
    offset: int = 0,
) -> List[TeachingResponse]:
    """教え一覧を取得（CEO専用）"""
    pass

@router.get("/teachings/{teaching_id}")
async def get_teaching(
    teaching_id: UUID,
) -> TeachingResponse:
    """教えを取得（CEO専用）"""
    pass

@router.put("/teachings/{teaching_id}")
async def update_teaching(
    teaching_id: UUID,
    updates: TeachingUpdate,
) -> TeachingResponse:
    """教えを更新（CEO専用）"""
    pass

@router.delete("/teachings/{teaching_id}")
async def deactivate_teaching(
    teaching_id: UUID,
) -> None:
    """教えを無効化（CEO専用）"""
    pass
```

---

## 11. 実装計画

### 11.1 フェーズ分け

| フェーズ | 内容 | 工数 | 依存関係 |
|---------|------|------|----------|
| **2D-0** | 脳アーキテクチャ本番有効化 | 1-2週間 | なし |
| **2D-1** | DBマイグレーション・基盤 | 1日 | 2D-0 |
| **2D-2** | CEO学習層実装 | 3-4日 | 2D-1 |
| **2D-3** | ガーディアン層実装 | 3-4日 | 2D-1 |
| **2D-4** | アラート機能実装 | 1-2日 | 2D-3 |
| **2D-5** | 脳統合・応答生成統合 | 2日 | 2D-2, 2D-4 |
| **2D-6** | テスト・検証 | 2-3日 | 2D-5 |
| **2D-7** | 本番デプロイ・監視 | 1日 | 2D-6 |

### 11.2 詳細タスク

#### Phase 2D-0: 脳アーキテクチャ本番有効化（1-2週間）

これは既に計画があるため、その計画に従う。
`~/.claude/plans/linear-questing-cosmos.md` 参照。

#### Phase 2D-1: DBマイグレーション・基盤（1日）

| タスク | 内容 | 担当 |
|--------|------|------|
| 2D-1-1 | マイグレーションSQL作成 | Claude |
| 2D-1-2 | マイグレーション実行 | Claude |
| 2D-1-3 | データクラス定義 | Claude |
| 2D-1-4 | 基本的なCRUD関数 | Claude |

#### Phase 2D-2: CEO学習層実装（3-4日）

| タスク | 内容 | 担当 |
|--------|------|------|
| 2D-2-1 | `lib/brain/ceo_learning.py` 作成 | Claude |
| 2D-2-2 | CEO会話検出ロジック | Claude |
| 2D-2-3 | 教え抽出プロンプト実装 | Claude |
| 2D-2-4 | 教え保存・検索ロジック | Claude |
| 2D-2-5 | ユニットテスト | Claude |

#### Phase 2D-3: ガーディアン層実装（3-4日）

| タスク | 内容 | 担当 |
|--------|------|------|
| 2D-3-1 | `lib/brain/guardian.py` 作成 | Claude |
| 2D-3-2 | MVV検証ロジック | Claude |
| 2D-3-3 | 選択理論検証ロジック | Claude |
| 2D-3-4 | SDT検証ロジック | Claude |
| 2D-3-5 | 既存教え矛盾検出 | Claude |
| 2D-3-6 | ユニットテスト | Claude |

#### Phase 2D-4: アラート機能実装（1-2日）

| タスク | 内容 | 担当 |
|--------|------|------|
| 2D-4-1 | アラート生成ロジック | Claude |
| 2D-4-2 | CEOへの通知機能 | Claude |
| 2D-4-3 | アラート解決フロー | Claude |
| 2D-4-4 | ユニットテスト | Claude |

#### Phase 2D-5: 脳統合・応答生成統合（2日）

| タスク | 内容 | 担当 |
|--------|------|------|
| 2D-5-1 | `SoulkunBrain`への統合 | Claude |
| 2D-5-2 | 応答生成への教え注入 | Claude |
| 2D-5-3 | 適用教えのログ記録 | Claude |
| 2D-5-4 | 統合テスト | Claude |

#### Phase 2D-6: テスト・検証（2-3日）

| タスク | 内容 | 担当 |
|--------|------|------|
| 2D-6-1 | E2Eテストシナリオ作成 | Claude |
| 2D-6-2 | 教え抽出精度テスト | Claude |
| 2D-6-3 | 矛盾検出精度テスト | Claude |
| 2D-6-4 | パフォーマンステスト | Claude |
| 2D-6-5 | セキュリティテスト | Claude |

#### Phase 2D-7: 本番デプロイ・監視（1日）

| タスク | 内容 | 担当 |
|--------|------|------|
| 2D-7-1 | デプロイ | Claude |
| 2D-7-2 | 監視設定 | Claude |
| 2D-7-3 | ドキュメント更新 | Claude |

---

## 12. テスト戦略

### 12.1 テストレベル

| レベル | 対象 | ツール | カバレッジ目標 |
|--------|------|--------|----------------|
| ユニットテスト | 各クラス・関数 | pytest | 80%以上 |
| 統合テスト | コンポーネント間連携 | pytest | 70%以上 |
| E2Eテスト | 全体フロー | 手動 + スクリプト | 主要シナリオ100% |

### 12.2 テストシナリオ

#### 12.2.1 CEO学習層テスト

```python
class TestCEOLearning:
    """CEO学習層のテスト"""

    def test_detect_ceo_conversation(self):
        """CEOとの会話を正しく検出"""
        pass

    def test_extract_teaching_simple(self):
        """シンプルな教えを抽出"""
        pass

    def test_extract_teaching_complex(self):
        """複雑な教えを抽出"""
        pass

    def test_extract_no_teaching(self):
        """教えがない会話から抽出しない"""
        pass

    def test_categorize_teaching(self):
        """教えを正しくカテゴリ分類"""
        pass

    def test_search_relevant_teachings(self):
        """関連する教えを検索"""
        pass
```

#### 12.2.2 ガーディアン層テスト

```python
class TestGuardian:
    """ガーディアン層のテスト"""

    def test_mvv_aligned_teaching(self):
        """MVVに沿った教えは合格"""
        pass

    def test_mvv_conflicting_teaching(self):
        """MVVに矛盾する教えを検出"""
        pass

    def test_choice_theory_aligned(self):
        """選択理論に沿った教えは合格"""
        pass

    def test_choice_theory_conflicting(self):
        """選択理論に矛盾する教えを検出"""
        pass

    def test_existing_conflict_detection(self):
        """既存教えとの矛盾を検出"""
        pass

    def test_alert_generation(self):
        """アラートを正しく生成"""
        pass
```

### 12.3 精度テスト

```python
class TestAccuracy:
    """精度テスト"""

    # テストデータセット
    TEACHING_EXTRACTION_TESTSET = [
        # (入力会話, 期待される教え数, 期待される内容)
        (
            "人は正しいから動くのではなく、心が動いたから動くんだよね。だから論理で説得しても意味がない。",
            1,
            {"statement_contains": "心が動いた", "category": "choice_theory"}
        ),
        (
            "今日は天気がいいね。",
            0,
            None
        ),
        # ... 100件程度
    ]

    CONFLICT_DETECTION_TESTSET = [
        # (教え, 期待される矛盾タイプ, 期待される深刻度)
        (
            "できない人はクビにすればいい",
            "mvv_mission",  # 可能性の解放に反する
            "high"
        ),
        (
            "目標は上から与えるものだ",
            "sdt_autonomy",  # 自律性に反する
            "medium"
        ),
        # ... 100件程度
    ]

    def test_extraction_accuracy(self):
        """教え抽出の精度が80%以上"""
        pass

    def test_conflict_detection_accuracy(self):
        """矛盾検出の精度が90%以上"""
        pass
```

---

## 13. 運用計画

### 13.1 監視

| 監視対象 | メトリクス | アラート条件 |
|---------|-----------|-------------|
| 教え抽出 | 抽出成功率 | < 70% |
| 矛盾検出 | 検出率 | 異常増加/減少 |
| アラート | 未解決アラート数 | > 10件 |
| 応答時間 | 教え検索時間 | > 1000ms |

### 13.2 ログ

```python
# Cloud Loggingクエリ

# CEO学習ログ
resource.type="cloud_function"
resource.labels.function_name="chatwork-webhook"
"[CEO_LEARNING]"

# ガーディアンアラート
resource.type="cloud_function"
resource.labels.function_name="chatwork-webhook"
"[GUARDIAN_ALERT]"

# 教え適用ログ
resource.type="cloud_function"
resource.labels.function_name="chatwork-webhook"
"[TEACHING_APPLIED]"
```

### 13.3 定期レポート

```
毎週月曜 9:00 に菊地さんに以下をレポート:

📊 CEO学習・ガーディアン週次レポート

【今週の学習】
- 新規教え: X件
- 検証済み: X件
- アラート: X件（解決X件、未解決X件）

【教えの活用】
- 応答への適用: X回
- 役に立った評価: X回

【要確認】
- 未解決アラート: X件
  1. 「〇〇」について確認お願いします
  2. ...
```

---

## 14. リスクと対策

### 14.1 技術的リスク

| リスク | 影響 | 確率 | 対策 |
|--------|------|------|------|
| 教え抽出の精度が低い | 誤った教えを学習 | 中 | 確信度閾値、CEO確認機能 |
| 矛盾検出の誤検知 | CEOの信頼低下 | 中 | 閾値調整、グレーゾーン許容 |
| LLMコスト増加 | 運用コスト増 | 高 | キャッシュ、バッチ処理 |
| 応答時間の増加 | UX低下 | 中 | 並列処理、キャッシュ |

### 14.2 ビジネスリスク

| リスク | 影響 | 確率 | 対策 |
|--------|------|------|------|
| CEOがアラートを無視 | ガーディアンの形骸化 | 低 | 週次レポート、未解決件数表示 |
| 教えの矛盾が蓄積 | 一貫性のない応答 | 低 | 定期的な教えレビュー |
| 過学習 | 特定状況に偏った応答 | 中 | カテゴリバランス監視 |

---

## 15. 実装チェックリスト

### 15.1 Phase 2D-1: DB・基盤

- [ ] マイグレーションSQL作成
- [ ] マイグレーション実行（本番）
- [ ] `CEOTeaching` データクラス
- [ ] `GuardianAlert` データクラス
- [ ] 基本CRUD関数
- [ ] 10の鉄則チェック（organization_id等）

### 15.2 Phase 2D-2: CEO学習層

- [ ] `lib/brain/ceo_learning.py` 作成
- [ ] `CEOLearningService` クラス
- [ ] CEO会話検出ロジック
- [ ] 教え抽出プロンプト
- [ ] 教え保存ロジック
- [ ] 教え検索ロジック
- [ ] ユニットテスト（20件以上）
- [ ] `chatwork-webhook/lib/brain/` 同期

### 15.3 Phase 2D-3: ガーディアン層

- [ ] `lib/brain/guardian.py` 作成
- [ ] `GuardianService` クラス
- [ ] MVV検証ロジック
- [ ] 選択理論検証ロジック
- [ ] SDT検証ロジック
- [ ] 行動指針検証ロジック
- [ ] 既存教え矛盾検出
- [ ] ユニットテスト（30件以上）
- [ ] `chatwork-webhook/lib/brain/` 同期

### 15.4 Phase 2D-4: アラート機能

- [ ] アラート生成ロジック
- [ ] アラートメッセージテンプレート
- [ ] CEOへの通知（ChatWork）
- [ ] アラート解決フロー
- [ ] ユニットテスト（10件以上）

### 15.5 Phase 2D-5: 統合

- [ ] `SoulkunBrain` への統合
- [ ] `BrainIntegration` への統合
- [ ] 応答生成への教え注入
- [ ] 適用教えのログ記録
- [ ] 統合テスト

### 15.6 Phase 2D-6: テスト

- [ ] E2Eテストシナリオ
- [ ] 教え抽出精度テスト（80%以上）
- [ ] 矛盾検出精度テスト（90%以上）
- [ ] パフォーマンステスト（+500ms以内）
- [ ] セキュリティテスト

### 15.7 Phase 2D-7: デプロイ

- [ ] PR作成・レビュー
- [ ] Quality Checks パス
- [ ] 本番デプロイ
- [ ] 動作確認
- [ ] 監視設定
- [ ] CLAUDE.md 更新

---

## 付録

### A. 用語集

| 用語 | 説明 |
|------|------|
| 教え（Teaching） | CEOから学習した原則・方針・判断基準 |
| ガーディアン | MVV/組織論との整合性を検証する機能 |
| 聖域 | 変更できない基準（MVV、選択理論等） |
| アラート | 矛盾検出時のCEOへの通知 |
| 上書き（Override） | CEOがアラートを確認した上で教えを保存すること |

### B. 参考文献

1. 選択理論（Choice Theory）- William Glasser
2. 自己決定理論（Self-Determination Theory）- Deci & Ryan
3. サーバントリーダーシップ - Robert K. Greenleaf
4. 心理的安全性 - Amy Edmondson

### C. 変更履歴

| バージョン | 日付 | 変更者 | 変更内容 |
|-----------|------|--------|----------|
| v1.0.0 | 2026-01-27 | Claude Code | 初版作成 |

---

**この設計書は菊地雅克（代表取締役）の承認を得て実装を開始する。**
