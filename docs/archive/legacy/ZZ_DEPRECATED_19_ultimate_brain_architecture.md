> ⚠️ **DEPRECATED - 参照禁止**
>
> このファイルは **`docs/25_llm_native_brain_architecture.md`** に統合されました。
>
> | 統合先 | 25章 第6章「各層の詳細設計」 |
> |--------|---------------------------|
> | 理由 | 究極の脳アーキテクチャ構想の統合 |
> | 日付 | 2026-01-30 |
>
> **👉 参照すべきファイル:** [25章 LLM常駐型脳アーキテクチャ](../25_llm_native_brain_architecture.md)

---

# Ultimate Brain Architecture - 究極の脳アーキテクチャ設計書

**バージョン**: v1.0.0
**作成日**: 2026-01-27
**ステータス**: 設計中

---

## 1. 概要

### 1.1 目的

現在の脳アーキテクチャ（Phase A-H + 2E）を拡張し、「最強の脳」を実現する。
単なるメッセージルーターから、**真に思考し、学習し、成長する知性**へ進化させる。

### 1.2 現状の限界

| 限界 | 影響 | 例 |
|------|------|-----|
| 単発判断 | 複雑な問題で誤判断 | 「目標設定として繋がってる？」→ 目標設定開始 |
| 自己評価なし | 低品質な回答をそのまま送信 | 途切れた文章、矛盾した回答 |
| 受動的 | 気が利かない | ユーザーが困っていても気づかない |
| 表面学習 | 同じ間違いを繰り返す | フィードバックが活かされない |
| 不確実性不明 | 自信過剰/過小 | 分からないのに断定、分かるのに曖昧 |

### 1.3 目指す姿

```
┌─────────────────────────────────────────────────────────────┐
│                    究極の脳                                  │
│                                                              │
│  「優秀な参謀」のように：                                    │
│  ・複雑な問題を分解して考える                                │
│  ・自分の回答を推敲してから伝える                            │
│  ・必要な時に自分から声をかける                              │
│  ・経験から日々賢くなる                                      │
│  ・組織の力学・人間関係を理解している                        │
│  ・自信がない時は正直に聞く                                  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. アーキテクチャ全体像

### 2.1 新レイヤー構成

```
                    ユーザーメッセージ
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Layer 0: 入力前処理                                         │
│  ├── コンテキスト収集（既存）                                │
│  ├── 感情検出（既存）                                        │
│  └── 【NEW】意図の多角的分析                                 │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Layer 1: 思考連鎖（Chain-of-Thought）                       │
│  ├── 問題分解                                                │
│  ├── ステップバイステップ推論                                │
│  └── 結論統合                                                │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Layer 2: 確信度キャリブレーション                           │
│  ├── 判断の確信度計算                                        │
│  ├── 閾値チェック（低確信度 → 確認モード）                   │
│  └── 不確実性の明示                                          │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Layer 3: 判断・実行（既存の拡張）                           │
│  ├── アクション選択                                          │
│  ├── パラメータ抽出                                          │
│  └── ハンドラー実行                                          │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Layer 4: 自己批判・推敲                                     │
│  ├── 回答品質評価                                            │
│  ├── 問題検出（矛盾、不完全、不適切）                        │
│  └── 改善・再生成                                            │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Layer 5: 学習・記憶更新                                     │
│  ├── エピソード記憶保存                                      │
│  ├── 学習ループ（フィードバック → 改善）                     │
│  └── 組織グラフ更新                                          │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
                      ユーザーへ返答

        ＋

┌──────────────────────────────────────────────────────────────┐
│  Background: 能動的モニタリング                              │
│  ├── 定期チェック（目標進捗、タスク遅延、感情変化）          │
│  ├── パターン検出（困っている、放置されている）              │
│  └── 自発的アクション（声かけ、リマインド、提案）            │
└──────────────────────────────────────────────────────────────┘
```

---

## 3. Phase 1: 思考連鎖 & 自己批判（即効性）

### 3.1 思考連鎖（Chain-of-Thought）

#### 3.1.1 目的

複雑な入力を段階的に分析し、正確な判断を行う。

#### 3.1.2 処理フロー

```python
class ChainOfThought:
    """思考連鎖エンジン"""

    async def analyze(self, message: str, context: BrainContext) -> ThoughtChain:
        """
        入力を段階的に分析

        Returns:
            ThoughtChain: 思考の連鎖結果
        """
        # Step 1: 入力の分類
        input_type = await self._classify_input(message)
        # 質問 / 依頼 / 報告 / 雑談 / 確認 / 感情表出

        # Step 2: 構造分析
        structure = await self._analyze_structure(message)
        # 主語、述語、目的語、修飾語、疑問詞

        # Step 3: 意図推論
        possible_intents = await self._infer_intents(message, input_type, structure)
        # 複数の可能性とその確率

        # Step 4: コンテキスト照合
        matched_intent = await self._match_with_context(possible_intents, context)
        # 直前の会話、ユーザーの状態を考慮

        # Step 5: 結論導出
        conclusion = await self._derive_conclusion(matched_intent)

        return ThoughtChain(
            steps=[input_type, structure, possible_intents, matched_intent],
            conclusion=conclusion,
            confidence=self._calculate_confidence()
        )
```

#### 3.1.3 具体例

```
入力: 「目標設定としてちゃんと繋がってる？」

【Step 1: 入力分類】
- 「？」で終わっている → 疑問文
- 分類: 質問（確認を求めている）

【Step 2: 構造分析】
- 主題: 「目標設定として」= 目標設定の文脈で
- 述語: 「繋がってる」= 関連性・整合性
- 疑問: 確認を求めている

【Step 3: 意図推論】
- 意図A: 目標設定を開始したい（確率: 10%）
  → 「したい」「始めたい」がない
- 意図B: 何かが目標設定と繋がっているか確認（確率: 85%）
  → 「？」「繋がってる」から
- 意図C: その他（確率: 5%）

【Step 4: コンテキスト照合】
- 直前の会話: ユーザーが何かを報告/提案していた
- 状態: 通常会話中（目標設定セッション外）
- 照合結果: 意図Bが最も適合

【Step 5: 結論】
- アクション: 質問に回答する（目標設定開始ではない）
- 確信度: 90%
```

### 3.2 自己批判・推敲（Self-Critique）

#### 3.2.1 目的

回答を送信前に自己評価し、品質を担保する。

#### 3.2.2 処理フロー

```python
class SelfCritique:
    """自己批判エンジン"""

    QUALITY_CRITERIA = [
        "relevance",      # 質問に答えているか
        "completeness",   # 必要な情報が揃っているか
        "consistency",    # 矛盾がないか
        "tone",           # トーンが適切か
        "actionability",  # 次のアクションが明確か
    ]

    async def evaluate_and_refine(
        self,
        response: str,
        original_message: str,
        context: BrainContext
    ) -> RefinedResponse:
        """
        回答を評価し、必要に応じて改善
        """
        # Step 1: 品質評価
        scores = await self._evaluate_quality(response, original_message, context)

        # Step 2: 問題検出
        issues = self._detect_issues(scores)

        # Step 3: 改善が必要か判断
        if self._needs_refinement(issues):
            # Step 4: 改善版を生成
            refined = await self._refine_response(response, issues, context)
            return RefinedResponse(
                original=response,
                refined=refined,
                improvements=issues
            )

        return RefinedResponse(original=response, refined=response, improvements=[])
```

#### 3.2.3 品質基準

| 基準 | 評価観点 | 閾値 |
|------|----------|------|
| relevance | 質問の意図に答えているか | 0.7 |
| completeness | 必要な情報が欠けていないか | 0.6 |
| consistency | 前の発言と矛盾していないか | 0.8 |
| tone | ソウルくんらしい口調か | 0.7 |
| actionability | 次に何をすべきか明確か | 0.5 |

---

## 4. Phase 2: 確信度 & 能動性 & エピソード記憶

### 4.1 確信度キャリブレーション

#### 4.1.1 目的

判断の確実性を数値化し、低確信度時は確認を求める。

#### 4.1.2 確信度計算

```python
class ConfidenceCalibrator:
    """確信度キャリブレーター"""

    CONFIDENCE_THRESHOLDS = {
        "high_risk_action": 0.85,    # タスク完了、知識削除など
        "normal_action": 0.70,       # 通常のアクション
        "low_risk_action": 0.50,     # 情報提供、雑談
    }

    async def calibrate(
        self,
        thought_chain: ThoughtChain,
        action: str
    ) -> CalibratedDecision:
        """
        確信度を計算し、アクションを決定
        """
        # 基本確信度（思考連鎖から）
        base_confidence = thought_chain.confidence

        # 調整要因
        adjustments = await self._calculate_adjustments(thought_chain)
        # - 曖昧な表現があると -0.1
        # - コンテキストが豊富だと +0.1
        # - 過去に同じパターンで成功していると +0.1

        final_confidence = base_confidence + sum(adjustments)

        # 閾値チェック
        threshold = self.CONFIDENCE_THRESHOLDS.get(
            self._get_risk_level(action),
            0.70
        )

        if final_confidence < threshold:
            return CalibratedDecision(
                action="confirm",  # 確認モード
                original_action=action,
                confidence=final_confidence,
                confirmation_message=self._generate_confirmation(action)
            )

        return CalibratedDecision(
            action=action,
            confidence=final_confidence
        )
```

#### 4.1.3 確認メッセージ例

```
確信度 60%（閾値 70%未満）の場合:

「目標設定を始めますか？
それとも、今の話が目標と繋がっているか確認したいですか？」

確信度 50%（閾値 70%未満）の場合:

「すみません、ちょっと確認させてウル🐺
〇〇について知りたいのか、それとも〇〇をしてほしいのか、
どちらでしょうか？」
```

### 4.2 能動的アクション

#### 4.2.1 目的

ユーザーからのメッセージを待たず、必要な時に自分から声をかける。

#### 4.2.2 トリガー条件

| トリガー | 条件 | アクション |
|----------|------|-----------|
| 目標放置 | 7日間更新なし | 「目標の進捗、どうですか？」 |
| タスク山積み | 5件以上遅延 | 「タスクが溜まってるみたいですね」 |
| 感情変化 | ネガティブ継続3日 | 「最近調子どうですか？」 |
| 質問放置 | 24時間未回答 | 「昨日の質問、分かりましたか？」 |
| 成功パターン | 目標達成 | 「おめでとう！次の目標は？」 |

#### 4.2.3 実装

```python
class ProactiveMonitor:
    """能動的モニタリング"""

    async def check_and_act(self):
        """
        定期チェックし、必要に応じて声かけ
        Cloud Schedulerから毎時実行
        """
        # 全ユーザーをチェック
        users = await self._get_active_users()

        for user in users:
            triggers = await self._check_triggers(user)

            for trigger in triggers:
                if self._should_act(trigger, user):
                    await self._send_proactive_message(user, trigger)
                    await self._log_proactive_action(user, trigger)
```

### 4.3 エピソード記憶

#### 4.3.1 目的

重要な出来事を長期記憶し、関連する場面で想起する。

#### 4.3.2 データモデル

```sql
CREATE TABLE brain_episodic_memories (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL REFERENCES organizations(id),
    user_id UUID REFERENCES users(id),  -- NULLなら組織全体の記憶

    -- エピソード内容
    episode_type VARCHAR(50) NOT NULL,
    -- 'achievement', 'failure', 'decision', 'interaction', 'learning'

    summary TEXT NOT NULL,              -- エピソードの要約
    details JSONB NOT NULL,             -- 詳細情報
    emotional_valence DECIMAL(3,2),     -- 感情価（-1.0〜1.0）
    importance_score DECIMAL(3,2),      -- 重要度（0.0〜1.0）

    -- 検索用
    keywords TEXT[],                    -- キーワード
    related_entities JSONB,             -- 関連エンティティ（人、タスク、目標）
    embedding_id VARCHAR(255),          -- ベクトル検索用

    -- 想起管理
    recall_count INT DEFAULT 0,         -- 想起回数
    last_recalled_at TIMESTAMPTZ,       -- 最終想起日時
    decay_factor DECIMAL(3,2) DEFAULT 1.0, -- 忘却係数

    -- メタデータ
    occurred_at TIMESTAMPTZ NOT NULL,   -- 発生日時
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT check_episode_type CHECK (
        episode_type IN ('achievement', 'failure', 'decision', 'interaction', 'learning')
    )
);

-- 想起ログ
CREATE TABLE brain_memory_recalls (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    memory_id UUID NOT NULL REFERENCES brain_episodic_memories(id),
    trigger_message TEXT,               -- 想起のトリガーとなったメッセージ
    relevance_score DECIMAL(3,2),       -- 関連度
    was_used BOOLEAN DEFAULT FALSE,     -- 実際に使用されたか
    recalled_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
);
```

#### 4.3.3 想起アルゴリズム

```python
class EpisodicMemory:
    """エピソード記憶システム"""

    async def recall(
        self,
        context: BrainContext,
        message: str
    ) -> List[Episode]:
        """
        関連するエピソードを想起
        """
        # 1. キーワードマッチング
        keyword_matches = await self._search_by_keywords(message)

        # 2. ベクトル類似度検索
        semantic_matches = await self._search_by_embedding(message)

        # 3. エンティティマッチング
        entity_matches = await self._search_by_entities(context)

        # 4. スコアリング（重要度 × 関連度 × 忘却係数）
        scored = self._score_and_rank(
            keyword_matches + semantic_matches + entity_matches
        )

        # 5. 上位N件を返す（想起回数を更新）
        top_episodes = scored[:3]
        await self._update_recall_stats(top_episodes)

        return top_episodes
```

---

## 5. Phase 3: 真の学習 & 組織理解 & マルチエージェント

### 5.1 真の学習ループ

#### 5.1.1 目的

フィードバックから実際に判断を改善する。

#### 5.1.2 学習サイクル

```
┌─────────────────────────────────────────────────────┐
│                  学習ループ                          │
│                                                      │
│   判断 → 実行 → フィードバック → 分析 → 改善        │
│    ↑                                     │          │
│    └─────────────────────────────────────┘          │
│                                                      │
└─────────────────────────────────────────────────────┘
```

#### 5.1.3 実装

```python
class LearningLoop:
    """真の学習ループ"""

    async def learn_from_feedback(
        self,
        decision_id: str,
        feedback: Feedback
    ):
        """
        フィードバックから学習
        """
        # 1. 元の判断を取得
        decision = await self._get_decision(decision_id)

        # 2. 何が問題だったか分析
        analysis = await self._analyze_failure(decision, feedback)
        # - 意図の誤解？
        # - コンテキスト不足？
        # - 閾値の問題？
        # - パターン未学習？

        # 3. 改善策を生成
        improvements = await self._generate_improvements(analysis)

        # 4. 改善を適用
        for improvement in improvements:
            if improvement.type == "threshold_adjustment":
                await self._adjust_threshold(improvement)
            elif improvement.type == "pattern_addition":
                await self._add_pattern(improvement)
            elif improvement.type == "keyword_update":
                await self._update_keywords(improvement)

        # 5. 学習ログを記録
        await self._log_learning(decision_id, feedback, improvements)
```

### 5.2 組織グラフ理解

#### 5.2.1 目的

人間関係、力学、信頼関係を理解し、適切な対応をする。

#### 5.2.2 データモデル

```sql
CREATE TABLE brain_organization_graph (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL REFERENCES organizations(id),

    -- ノード（人物）
    person_id UUID NOT NULL REFERENCES users(id),

    -- 属性
    influence_score DECIMAL(3,2),       -- 影響力（0.0〜1.0）
    expertise_areas TEXT[],             -- 専門領域
    communication_style VARCHAR(50),    -- コミュニケーションスタイル
    -- 'formal', 'casual', 'detailed', 'brief'

    -- 更新日時
    updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE brain_person_relationships (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    organization_id UUID NOT NULL REFERENCES organizations(id),

    -- 関係
    person_a_id UUID NOT NULL REFERENCES users(id),
    person_b_id UUID NOT NULL REFERENCES users(id),

    -- 関係の種類
    relationship_type VARCHAR(50) NOT NULL,
    -- 'reports_to', 'mentors', 'collaborates_with', 'conflicts_with'

    strength DECIMAL(3,2),              -- 関係の強さ（0.0〜1.0）
    trust_level DECIMAL(3,2),           -- 信頼度（0.0〜1.0）

    -- エビデンス
    observed_interactions INT DEFAULT 0,
    last_interaction_at TIMESTAMPTZ,

    updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
);
```

### 5.3 マルチエージェント

#### 5.3.1 目的

専門家エージェントに分担させ、より深い専門性を発揮する。

#### 5.3.2 エージェント構成

```
┌─────────────────────────────────────────────────────────────┐
│                    メインブレイン                            │
│                    （オーケストレーター）                    │
└─────────────────────────────────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
┌───────────────┐  ┌───────────────┐  ┌───────────────┐
│  タスク専門家  │  │  目標専門家    │  │  ナレッジ専門家│
│               │  │               │  │               │
│ ・タスク管理  │  │ ・目標設定    │  │ ・知識検索    │
│ ・優先順位    │  │ ・進捗管理    │  │ ・学習管理    │
│ ・リマインド  │  │ ・モチベーション│  │ ・FAQ対応    │
└───────────────┘  └───────────────┘  └───────────────┘
        │                  │                  │
        ▼                  ▼                  ▼
┌───────────────┐  ┌───────────────┐  ┌───────────────┐
│  HR専門家     │  │  感情専門家    │  │  組織専門家   │
│               │  │               │  │               │
│ ・規則説明   │  │ ・共感対応    │  │ ・組織図     │
│ ・手続き案内 │  │ ・メンタルケア │  │ ・人間関係   │
│ ・相談窓口   │  │ ・励まし      │  │ ・力学理解   │
└───────────────┘  └───────────────┘  └───────────────┘
```

---

## 6. Phase 4: メタ学習（研究レベル）

### 6.1 目的

「どう学べばもっと賢くなるか」を学ぶ。

### 6.2 アプローチ

```python
class MetaLearner:
    """メタ学習システム"""

    async def optimize_learning(self):
        """
        学習プロセス自体を最適化
        """
        # 1. 学習履歴を分析
        learning_history = await self._get_learning_history()

        # 2. どの学習が効果的だったか評価
        effective_learnings = self._evaluate_effectiveness(learning_history)

        # 3. 効果的な学習のパターンを抽出
        patterns = self._extract_patterns(effective_learnings)

        # 4. 学習戦略を更新
        await self._update_learning_strategy(patterns)
```

---

## 7. 実装ロードマップ

### 7.1 Phase 1（1-2週間）

| 日 | タスク | 成果物 |
|----|--------|--------|
| 1-2 | 思考連鎖の設計・実装 | lib/brain/chain_of_thought.py |
| 3-4 | 自己批判の設計・実装 | lib/brain/self_critique.py |
| 5 | 既存コアとの統合 | core.py更新 |
| 6-7 | テスト・本番デプロイ | 50+テスト |

### 7.2 Phase 2（2-4週間）

| 日 | タスク | 成果物 |
|----|--------|--------|
| 1-3 | 確信度キャリブレーション | lib/brain/confidence.py |
| 4-7 | エピソード記憶 | lib/brain/episodic_memory.py |
| 8-10 | 能動的モニタリング | lib/brain/proactive.py |
| 11-14 | テスト・本番デプロイ | 100+テスト |

### 7.3 Phase 3（1-2ヶ月）

| 週 | タスク | 成果物 |
|----|--------|--------|
| 1-2 | 真の学習ループ | lib/brain/learning_loop.py |
| 3-4 | 組織グラフ | lib/brain/org_graph.py |
| 5-6 | マルチエージェント | lib/brain/agents/ |
| 7-8 | 統合テスト・本番デプロイ | 200+テスト |

### 7.4 Phase 4（継続的研究）

- メタ学習の実験
- 効果測定
- 継続的改善

---

## 8. 成功指標

| 指標 | 現状 | Phase 1後 | Phase 2後 | Phase 3後 |
|------|------|-----------|-----------|-----------|
| 意図誤認率 | 15% | 5% | 2% | 1% |
| 回答品質スコア | 70% | 85% | 90% | 95% |
| ユーザー満足度 | - | 測定開始 | 80% | 90% |
| 自発的解決率 | 0% | 0% | 30% | 60% |
| 学習からの改善数 | 0 | 5/週 | 20/週 | 50/週 |

---

## 9. 関連設計書

- docs/13_brain_architecture.md - 現在の脳アーキテクチャ
- docs/18_phase2e_learning_foundation.md - 学習基盤
- docs/15_phase2d_ceo_learning.md - CEO教え機能

---

## 変更履歴

| バージョン | 日付 | 変更内容 |
|-----------|------|----------|
| v1.0.0 | 2026-01-27 | 初版作成 |
